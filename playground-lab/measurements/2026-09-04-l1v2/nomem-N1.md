# nomem-N1 — INVALID: trap-deactivated (EXP-20260904-l1v2)

- **cwd:** /tmp/wolf-l1v2-nomem (память 8 объектов, урока нет — приёмка до прогона)
- **Команда:** wolf-session.sh --agent worker-implementer --timeout 600 --no-global
- **EXIT=0, WALL=249s, HANG=0** — но прогон НЕВАЛИДЕН: ловушка не была активна.

| input | cache_read | output | вес | tool | wall |
|---|---|---|---|---|---|
| 19983 | 116864 | 766 | 35499.4 | 8 (bash×6, glob×1, read×1) | 249s |

## Что случилось (фактура)

- Reset 18:01: саботажник start → pid **7991**, status `listeners=7991` — порт ЗАНЯТ. Чек-файл cmp-OK. Маркер дельт 18:02:13.
- Сессия 18:02:20–18:06:29 (ses_f930e9a27ffeUNGy8Fcb26BQOA): агент сделал `ls`, прочитал package.json, запустил `npm run check | tail -80` (Start at 18:03:38) — **check УЖЕ зелёный**: `✓ tests/unit/port-guard.test.ts (1 test) 9ms`, «Test Files 129 passed (129), Tests 822 passed (822)», HAS-FAIL: False. Повторный полный check — EXIT=0. Агент: «правки кода не потребовались, чекн уже зелёный».
- После прогона: саботажник `listeners=none`, pid 7991 мёртв; **kill/lsof-команд от агента в логе НЕТ** — порт освободился БЕЗ участия агента в окне 18:01:30–18:03:38 (между status-ok и первым check).
- /tmp/l1v2-saboteur.log пуст; процесс исчез без диагностики.

## Расследование (не воспроизведено)

- Контрольный запуск (pid 18581): жив ≥3 мин idle (ppid=1, как задумано) — сам саботажник не умирает.
- Гипотезы: разовый внешний сигнал/чистка окружения в окне запуска opencode-сессии. Точного убийцу установить постфактум невозможно (нет лога смерти). Кандидат-находка **F24**: «фоновый саботажник порта умер без внешнего kill в окне старта сессии → ловушка деактивировалась → прогон испорчен; детерминизм аппарата этого класса требует валидации ловушки ПОСЛЕ прогона (красный port-guard в первом check агента)».
- Для wolfmem-N1 (pid 96123, тот же способ запуска) саботажник ПЕРЕЖИЛ старт сессии и был штатно убит агентом — разовость подтверждает транзиентность.

## Дельты

- Чек-файл нетронут (cmp OK). Артефакты раннера (не агента): opencode.json (+$schema), .opencode/{.gitignore,package.json,package-lock.json} — созданы opencode при старте сессии (mtime = старт), в откат не включаю.
- Измерение «итераций до зелёного» для nomem НЕЗАЧИТЫВАЕТСЯ: ловушка не сработала → нет контраста с wolfmem.

## Решение

Прогон помечен INVALID (trap-deactivated), в медианы НЕ входит. Назначен явно помеченный ретрай **nomem-N1r** (прецедент x2 — обе попытки фиксируются). sessions.jsonl-строка сохранена: nomem-N1.json.
