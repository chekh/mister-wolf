# FINDINGS — реестр проблем, найденных на полигоне

Живой реестр дефектов и трения из догфудинга `playground/` (расходный
pristine-инстанс). Сырой протокол сценариев — черновик `playground/MANUAL-NOTES.md`
до переноса в карты экспериментов. F-нумерация сквозная с момента основания.
Статусы: `open` → `in-brief` (диспетчено) → `fixed` / `rejected` (с причиной) / `documented`.

| ID | Дата | Сценарий | Тип | Описание | Статус |
|---|---|---|---|---|---|
| F1 | 2026-09-01 | Установка | UX/DOC | Глобальный `wolf` оказался симлинком на dev-копию (`mr-wolf@npm:mister-wolf -> /Users/chekh/Development/mister-wolf`) — версия всегда равна рабочему дереву. Мануалу нужен способ отличать линк от реальной установки (стрелка `->` в `npm ls -g`). | documented (мануал) |
| F2 | 2026-09-01 | Установка | PREMISE WRONG | ПРЕМИС НАЙДКИ БЫЛА ОШИБОЧНА: `mr-wolf` в npm — ЧУЖОЙ пакет (maintainer julian, не zzorgian), просто похожее имя, НЕ легаси wolf. Попытка deprecate через CI (trusted publishing, пер-пакетный OIDC) и локально (npm login + deprecate) остановлена: CI дал E404 (нет линковки чужого пакета — кстати, это и спасло), локально EOTP 2FA; проверка `npm view mr-wolf deprecated` — пусто, пакет не задет. Для мануала: наш пакет — ТОЛЬКО `mister-wolf`; `npm i -g mr-wolf` ставит чужой софт — предупредить в README. | resolved (wontfix: чужой пакет) |
| F3 | 2026-09-01 | Обновление | DOC | Путь обновления `npm rm -g` → `npm i -g mister-wolf@latest` работает чисто (28 пакетов). В мануал: ожидаемый вес, проверка `npm ls -g mister-wolf` без `->`. | documented (мануал) |
| F4 | 2026-09-01 | `wolf init` | **BUG** | Первый init свежего проекта НЕ пишет платформенный конфиг (`opencode.json` с MCP): «platform configs: skipped — no platform detected» — детекция не видит каталог `.opencode/`, который сама создаёт. MCP подключается только при ВТОРОМ init. Воспроизведён дважды (в т.ч. на чистой площадке). Чинить: детекция после рендера или дефолт opencode. | fixed (2.0.0) |
| F5 | 2026-09-01 | `wolf init` | UX | Лог init не показывает имена скиллов: 13 строк «SKILL.md created». Писать имя/путь. | fixed (2.1.0) |
| F6 | 2026-09-01 | `wolf init` | UX | Misleading-сообщения: «platform configs: skipped» звучит как «агенты не установлены»; «Restart your agent platform…» печатается даже когда MCP не настроен. | fixed (2.1.0) |
| F7 | 2026-09-01 | `wolf init` | DOC | Init не сообщает про `opencode.json`/шаг подключения MCP. Подсказка в выводе init и в help. | fixed (2.0.0) |
| F8 | 2026-09-01 | `wolf init` | **Q владельцу** | Init автоматически делает полный scan: 97–98 документов легли в `.wolf/memory/shared/documents/` (107 файлов в «пустой» памяти). Владелец расценил как замусоривание/«чужую память». Предложение: scan при init опциональным (`--scan`) или лёгким; полный scan — отдельная команда. | fixed (2.0.0) |
| F9 | 2026-09-01 | Поиск/память | **BUG** (подтверждено владельцем) | Авто-документы ломают нейминг-паттерн и поиск: id = `doc_<путь_с_подчёркиваниями>` (без `mem_`, даты, хеша — нарушение канона `mem_ГГГГММДД_slug_hash`). Путь стал токенами FTS: `wolf search "md"` → **98/98** документов, `"docs"` → 89, `"concept"` → 7 (в т.ч. по пути, не по содержимому); контентный поиск («supersede» → 2) работает. Поиск по служебным словам (md, docs, src, wolf) даёт стеношум. Чинить: id по канону, в индекс — заголовок/контент, путь — метаданными (не токенами); неиндексируемые сегменты пути исключить. NB: вариант D (в main, не издан) усилит эффект — сегменты пути станут полными токенами. | fixed (2.1.0: канон id + migrate doc-ids) |
| F10 | 2026-09-01 | Поиск/память | UX | Несогласованность имён типа: в config.yaml таксономия `memory_types.core.document`, фактически тип `document-ref`; `wolf list --type document` молча возвращает пусто вместо ошибки/подсказки валидных типов. | fixed (2.1.0: alias + подсказка типов) |
| F11 | 2026-09-01 | bootstrap+recap | **BUG** | Правила в статусе `accepted` НЕ попадают в `wolf recap` («Active rules» пуст при 3 accepted). Расхождение lifecycle правил (proposed→accepted) с фильтром recap (ждёт active?). Следствие: следующая сессия не увидит принятые конвенции в инъекциях. | fixed (2.0.0) |
| F12 | 2026-09-01 | Апгрейд | UX/процесс | Глобальное окружение мутировалось во время релизной задачи (до апгрейд-теста уже стояла 2.0.0 вместо 1.1.0) — смоук через глобальный бинарь без документирования. Рецепт: изолированный npm prefix для релизных смоуков; состояние установки проверять `readlink -f $(which wolf)` + `npm ls -g mister-wolf`. | process (урок mem_…29eedb) |

## Принятые решения по находкам

(заполняется по мере триажа владельцем)
| F13 | 2026-09-01 | CLI, любой cwd | UX | `wolf` в удалённом каталоге падает сырым стеком Node (`ENOENT uv_cwd` из `process.cwd()` в cli-entry.js runCli). Wolf сам удаляет worktrees → пользователь внутри удалённого каталога получает непонятный краш. Фикс: guard на входе CLI — catch и однострочник «текущий каталог не существует, cd куда-нибудь». | fixed (2.0.1) |
| F16 | 2026-09-01 | Тестовая инфра | **BUG** | `npm run check` (vitest) пишет ~5 мёртвых записей в глобальный реестр за прогон (doctor их прунит). Тот же класс, что F14, но в vitest-тестах. Кандидат: изоляция XDG для vitest-окружения (setup-файл). | fixed (2.1.0: vitest setup XDG) |
| F14 | 2026-09-01 | scripts/bench | **BUG** | Бенч-харнесс не изолирован и не убирается: `b2-bootstrap.sh`/`lib.sh` делают `wolf init` во временных /tmp/wolf-bench.* → записи в глобальном реестре навсегда (144 утюга), каталоги не удаляются. Фикс: WOLF_HOME/XDG-изоляция для bench + `trap rm -rf EXIT`. Разовая чистка сделана 01.09. | fixed (2.0.1) |
| F15 | 2026-09-01 | Онбординг / opencode-adapter | **BUG** | init не пишет `subagent_depth` в opencode.json → платформенный дефолт 1 ломает трёхуровневую схему: executor-lead не может спавнить worker-* («свежий субагент + ревью» → self-ревью). Главный репо работает, потому что там ключ выставлен руками (subagent_depth: 2). Фикс: adapter мерджит subagent_depth: 2 по образцу default_agent (§6.1: absent → set, занят → untouched + needs-fix). | fixed (2.0.1) |
