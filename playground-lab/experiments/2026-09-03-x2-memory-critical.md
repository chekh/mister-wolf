# EXP-20260903-x2: память-критичная задача (X2)

- Статус: **analyzed** (6/6 прогонов выполнены, но nomem-N1 испорчен F21+F22 → асимметричная выборка N=3 vs N=2; concluded невозможно)
- Дата: 2026-09-04 (ретрай; первая попытка 2026-09-03 отменена — hang первого opencode-прогона при изолированном HOME/OPENCODE_CONFIG, изоляция снята в ретрае, заменена F20-проверками постфактум)
- Гипотеза: релевантный урок в памяти (english-surface gate: покрывает только `src/adapters/**.ts` строковые литералы, docs/**/*.md не покрываются) снижает стоимость задачи «создай docs/notes/x2-note.md + зелёный npm run check» — весовые токены, итерации, gate-контакт. Карточка X2 в `registry/scenarios.md`.

## Сетап

| Вариант | cwd | Код | Память .wolf | Отличие |
|---|---|---|---|---|
| WOLF-MEM | playground/ (HEAD e40504e) | e40504e | **113 объектов, урок mem_20260903_…_2b589c присутствует** (`search "english"` → находка) | + урок |
| NO-MEM | /tmp/wolf-x2-nomem | git archive playground HEAD e40504e (побайтово идентичен, diff -rq = 0) + фильтр волчьих артефактов + init | **8 объектов (дефолт init), урока НЕТ** (`search "english"` → 0 results) | база |

Симметрично в обоих: init-набор 6 агентов / 13 скиллов / 3 команды / 2 плагина ( wolf-router, wolf-session-start 'recap'-митигация), AGENTS.md, opencode.json (mcp.wolf, default_agent, subagent_depth=2), dist-симлинк → main dist, модель pinned `zai-coding-plan/glm-5.2`, запуск `--agent worker-implementer`, headless `--format json`, единый источник кода = playground HEAD e40504e (устранён дрейф версий попытки 1: nomem резали из main → 866 тестов vs 821; теперь 821=821, baseline check зелёный в обеих, ~100s). Единственная переменная — наличие урока в .wolf.

## Метод измерения

Как в X1: весовые токены = input + 0.1×cache_read + 5×output из SQLite opencode (`session.tokens_*`, 1 сессия/прогон); wall; tool-вызовы (`"type":"tool_use"` в JSONL stdout); итерации (`npm run check` в tool-вызовах, включая rtk_run_command-обёртки; write/edit по пути x2-note.md); gate-контакт (кириллица в первой записанной версии; чтение check-english-surface.mjs / package.json). Медианы N=3 (NO-MEM валидно N=2 — см. F21/F22). Интерливинг wolfmem→nomem×3, hang-guard 480s, freshness docs/notes перед каждым прогоном. Сверка гейта с исходником: `scripts/check-english-surface.mjs` — `const ROOT = 'src/adapters'`, флагуется кириллица только в строковых литералах `.ts`-файлов (docs/**/*.md не покрываются); состав `npm run check` (package.json, дословно): `node scripts/check-english-surface.mjs && npm run format:check && npm run lint && npm run build && npm run test:run`.

## Сценарии

- `playground-lab/registry/scenarios.md` — X2 (память-критичная задача).

## Ожидаемое поведение

При верной гипотезе: WOLF-MEM пишет RU-заметку сразу (знает из урока, что docs не гейтится), меньше итераций check и/или ниже весовые токены; NO-MEM без урока — лишние итерации (перезапись в EN, повторный check) или дороже на выяснение гейта.

## Протокол

Прогоны (интерливинг, все 6 выполнены 2026-09-04):

| Прогон | EXIT | WALL | HANG | Сессия |
|---|---|---|---|---|
| wolfmem-N1 | 0 | 290s | 0 | ses_f9489b4a1ffex2BGYvrv1Rfubz |
| nomem-N1 | unknown | 480s | **1 (F21)** | ses_f947efd25ffeolLbwPG1cj9j75 (tokens 0\|0\|0 → F22) |
| wolfmem-N2 | 0 | 165s | 0 | ses_f947991deffe0e9qp8Pm1qGG0L |
| nomem-N2 | 0 | 135s | 0 | ses_f94765b52ffenur6r2Dr3kjysw |
| wolfmem-N3 | 0 | 140s | 0 | ses_f94736750ffek3BXhWSacjPaD7 |
| nomem-N3 | 0 | 160s | 0 | ses_f94705c25ffecrCWKh4fBGvDRJ |

Результаты (кэш_write=0 всюду; reasoning справочно: wolfmem 912–1727, nomem 1072–1083):

| Прогон | input | cache_read | output | вес | tool | wall | итерации check | RU-первая-попытка | читал гейт |
|---|---|---|---|---|---|---|---|---|---|
| wolfmem-N1 | 30507 | 135936 | 1029 | **49246** | 9 | 290s | 2 (rtk error → bash ok; успешных 1) | да | да |
| nomem-N1 | — | — | — | **недоступно (F21/F22)** | 0* | 480s | нет данных | нет данных | нет данных |
| wolfmem-N2 | 33582 | 138880 | 669 | **50815** | 7 | 165s | 1 | да | да |
| nomem-N2 | 29221 | 130112 | 710 | **45782** | 7 | 135s | 1 | да | да |
| wolfmem-N3 | 33725 | 111488 | 701 | **48379** | 7 | 140s | 1 | да | да |
| nomem-N3 | 29414 | 129856 | 751 | **46155** | 8 | 160s | 1 (через rtk, ok) | да | да |

\* JSONL nomem-N1 пуст (0 событий — при kill погиб небуферизованный stdout); в SQLite сессия создана через ~6 мин после запуска, tokens 0|0|0, артефакт не создан.

Медианы (WOLF-MEM N=3; NO-MEM N=2 — nomem-N1 исключён как невалидный):

| Медианы | WOLF-MEM | NO-MEM | Δ | Δ% |
|---|---|---|---|---|
| весовые токены | 49246 | 45968 | +3277 | **+7.1%** |
| input | 33582 | 29318 | +4265 | +14.5% |
| cache_read | 135936 | 129984 | +5952 | +4.6% |
| output | 701 | 731 | −29 | −4.0% |
| tool-вызовы | 7 | 7.5 | −0.5 | −6.7% |
| wall | 165s | 147.5s | +17.5s | +11.9% |
| итерации check (успешные) | 1 | 1 | 0 | 0% |

Качество/поведение (5 валидных прогонов): 5/5 артефакт создан, 5/5 check зелёный с первой успешной попытки (128 файлов / 821 тестов), 5/5 первая версия заметки — сразу на русском, 5/5 читали scripts/check-english-surface.mjs (и package.json). Различий в поведении между вариантами НЕТ: обе стороны выясняли гейт чтением скрипта, а не памятью; mr-wolf_* тулов в валидных прогонах 0 (урок не запрашивался). wolfmem-N1: rtk_run_command упал на `npm run check` (status error) → агент повторил через bash (his own FRICTION).

## Находки

- F21 (hang headless-прогона, повтор): nomem-N1 — 480s без единого события в stdout; сессия в БД создана ~6 мин после запуска, ответа модели не дождалась. Изоляция HOME в ретрае уже была снята — hang воспроизвёлся и без неё; на машине параллельно работали 6 интерактивных opencode-сессий (БД 7.7 GB), транзиентен (nomem-N2/N3 и все wolfmem — без hang). Вывод: F21 не связан с изоляцией профиля; вероятный фактор — конкурентная нагрузка окружения (БД/провайдер); для измерений нужно тихое окружение либо deadline > 480s с догоном.
- F22 (нет токен-статистики у убитой сессии): nomem-N1 → tokens 0|0|0, вес неопределим → асимметрия выборки (N=3 vs N=2), статус analyzed вместо concluded.
- F20-фактов НЕТ: до/после каждого nomem-прогона `.wolf/` = cache, config.yaml, memory, router.log (прирос только router.log — штатная работа плагина), `wolf list` стабильно 8. Контаминации nomem-памяти не выявлено (изоляция действительно не понадобилась).

## Вердикт

По цифрам (выжившие данные, асимметрия N=3 vs N=2): **экономии от урока нет** — медианный вес WOLF-MEM даже выше (+7.1%), итерации check равны (1=1 успешный), gate-контакт и язык первой версии идентичны (все 5 валидных прогонов сразу писали RU и читали гейт-скрипт). Причина по данным: задача не упирается в ловушку урока — docs/**/*.md гейтом не покрываются, RU-заметка проходит check в обоих вариантах, и обе стороны решают вопрос чтением скрипта за одинаковое число итераций. Урок не понадобился (вердикт по п.(б) на выживших данных). Формальный статус — analyzed: F21+F22 испортили один из шести прогонов (п.(в)), медианы NO-MEM посчитаны по N=2 и потому менее устойчивы; вывод «экономии нет» чувствителен к этому, но направление (+7% против −?) не даёт оснований ожидать скрытой экономии. Для строгого concluded нужен повтор с тихим окружением.

Сырьё: `measurements/2026-09-03-x2/` — x2-prompt.txt, setup-notes.md, memory-inventory-{wolfmem,nomem}.txt, {wolfmem,nomem}-N{1,2,3}.{json,md,stderr.log} (выжимки каждого прогона с артефактами дословно и цитатами финальных ответов — в *.md).
