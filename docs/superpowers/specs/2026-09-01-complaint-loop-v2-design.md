# Дизайн: жалобный контур v2 — жалоба объектом памяти, триаж диспетчером

|               |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Дата          | 2026-09-01                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Ревизия       | 1 — draft на мульти-линзовое ревью                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Статус        | На ревью (4 линзы: полнота / консистентность / реализуемость / безопасность); реализация — после аппрува владельца                                                                                                                                                                                                                                                                                                                                                                                                        |
| Источники     | Решение владельца `mem_20260901_zhalobnyy_kontur_v2_vorker_vnosit_zhalob_523b10` (замена d2b359 — не использовать); dogfood `.wolf/orchestration/report-2026-08-31-base-sets-phase-c-final.md` (T15.2: L2-пассивность 4/4, декларативный триггер не сработал); спека base-sets `docs/superpowers/specs/2026-08-31-base-sets-design.md` (§5.3 /complain, §7 посев); таксономия `src/domain/memory-types.ts`, переходы `src/domain/governance.ts` (ALLOWED_TRANSITIONS), CLI `src/adapters/cli/commands/memory-complain.ts` |
| Следующий шаг | Мульти-линзовое ревью → аппрув владельца (OPEN QUESTIONS §10) → план реализации → worktree                                                                                                                                                                                                                                                                                                                                                                                                                                |

## 1. Проблема

Жалобный контур v1 (complaint-protocol.md, посев wolf-init) требует от воркера
самому описать жалобу в тексте отчёта, самому вызвать Стюарда и самому следить,
что жалоба не потерялась. Dogfood фазы C (T15.2) показал: (а) воркеры
L2-пассивны — за 4 живых прогона ни один не стартовал контур сам (даже с
процедурным триггером контур замкнулся только после требования диспетчера);
(б) жалоба в тексте отчёта не запросить и не отследить — она живёт, пока
кто-то перечитывает отчёты; (в) решение о мутации принимает тот, кто подал
жалобу (воркер вызывает Стюарда напрямую), а не диспетчер.

Решение владельца (523b10): воркер вносит жалобу **структурированным объектом
в память Wolf** (правило, пруф, предложение); в отчёте — только указатель (id).
**Диспетчер** реагирует: проверяет открытые жалобы при приёмке каждого отчёта,
решает (починить инструкцией / вызвать Стюарда на мутацию / отклонить с
причиной) и переводит статус. Жалоба на самого диспетчера — выше. Мутации —
только через Стюарда.

## 2. Принятые решения

| #   | Решение                                                                                                                                                                                         | Альтернатива, которую отклонили                                                                                                                                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Новый core-тип `complaint` в таксономии: required-поля, суженный lifecycle, точный запрос диспетчера `wolf list --type complaint --status open` (§3)                                            | Переиспользовать `observation`: у него все поля optional (обязательность rule/evidence/proposal не enforce'ится), lifecycle FULL разрешает лишние переходы, запрос диспетчера смешивается с семантическими наблюдениями                |
| D2  | Lifecycle `open → resolved \| rejected` штатными переходами (ALLOWED_TRANSITIONS). «Взял в работу» — НЕ статус, а поле `triage`, которое диспетчер заполняет при первом рассмотрении (§3.3)     | Новый статус `acknowledged`: правка governance/schema ради семантики, которую покрывает поле; в текущих переходах `open → active` запрещён — «принять в работу» статусом невыразимо без расширения enum (вынесено в OPEN QUESTIONS Q2) |
| D3  | Подача — расширение существующего CLI `wolf complain` (опции `--rule/--evidence/--proposal`), объект создаётся со статусом `open`; relation `complain → about` и hot-signal Стюарду сохраняются | `wolf add --type lesson --tags complaint:...` (текущий ТРИГГЕР ЖАЛОБЫ в playbook'ах): нет валидации полей, нет relation, нет hot-signal, нет статуса open — диспетчеру нечем триажить                                                  |
| D4  | Решение о судьбе жалобы — централизованно за диспетчером (L1); воркер Стюарда не вызывает (изменение v1: в complaint-protocol v1 воркер вызывал сам)                                            | Воркер сам решает и сам вызывает Стюарда (v1): решение владельца явно передаёт триаж диспетчеру; следствие — упрощение роли воркера (меньше инициативы у пассивного L2)                                                                |
| D5  | SLA по приёмкам: жалоба живёт максимум N=3 приёмки (создание + 2 откладывания); счётчик `dispatch_ages`, при 2 откладываниях — обязательная эскалация координатору (§5.4)                       | SLA по календарному времени (`--stale`, 30 дней): единица «дни» не соответствует циклу приёмок — жалоба либо закрывается в текущем цикле, либо эскалируется                                                                            |
| D6  | Дедупликация — обязанность диспетчера при триаже: дубликат `rejected` + relation `related_to` на каноническую + `corroborations++` на канонической; воркер дубликаты не ищет (не тратит витки)  | Дедуп на стороне воркера (перед подачей `wolf search`): воркер L2 пассивен и не надёжен (T15.2), а поиск «похожей сути» — нетривиальное решение, не его работа                                                                         |

## 3. Объект жалобы

### 3.1 Тип и обоснование по таксономии

Новый core-тип `complaint` (добавление в `CORE_TAXONOMY_DECLS`,
`src/domain/memory-types.ts` — единственный источник истины о типах):

```ts
{
  name: 'complaint',
  lifecycle: ['open', 'resolved', 'rejected', 'obsolete', 'archived'],
  defaultStatus: 'open',            // как open-question
  subdirThread: 'notes',            // жалобы не привязаны к треду обязательного
  subdirShared: 'complaints',       // shared: диспетчерский список
  fields: {
    about:         { kind: 'string', required: true, min: 1 },  // target-агент/playbook/скилл
    rule:          { kind: 'string', required: true, min: 1 },  // какое правило/методика плохие
    evidence:      { kind: 'string', required: true, min: 1 },  // пруф: дословная цитата + что произошло
    proposal:      { kind: 'string', required: true, min: 1 },  // предложение автора
    triage:        { kind: 'string', optional: true },          // решение диспетчера (§5.3)
    resolution:    { kind: 'string', optional: true },          // исход: что сделано, ссылки
    dispatch_ages: { kind: 'int', default: 0 },                 // приёмок без исхода (SLA §5.4)
    corroborations:{ kind: 'int', default: 1 },                 // сколько жалоб слито в эту (дедуп §3.4)
  },
}
```

Обоснование против `observation` (текущий тип `wolf complain`): (а)
обязательные поля валидируются декларацией при записи — «жалоба без пруфа не
жалоба» enforce'ится кодом, а не соглашением; (б) диспетчерский запрос точный
и дешёвый (`--type complaint --status open`, ListFilters уже поддерживает
type+status); (в) lifecycle сужен до нужных переходов. Продюсеров `observation`
кроме `wolf complain` в коде нет (проверено grep по src/) — миграция
ограничивается самой командой; старые observation-жалобы (sandbox-догфудинг)
не мигрируются (НЕ-цель §11). Тип порождается и владельцем через /complain —
формат един для всех авторов (инвариант решения владельца).

### 3.2 Обязательные поля

| Поле       | Кто заполняет  | Содержание                                                                                                              |
| ---------- | -------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `about`    | автор          | target: agent-id (`worker-implementer`, `executor-lead`, `mr-wolf`, `steward`) или `skill:<имя>` / playbook mem-id      |
| `rule`     | автор          | какое правило плохо: указатель (пункт playbook'а, тег правила, формулировка) + чего оно требует                         |
| `evidence` | автор          | пруф: дословная цитата трения + что произошло (файл/тест/числа); пересказ вместо цитаты — жалоба не принимается в триаж |
| `proposal` | автор          | предложение: что изменить в правиле; воркер не обязан быть прав — обязан быть конкретен                                 |
| автор      | (`created_by`) | штатное поле; воркер передаёт фактического исполнителя, диспетчер/владелец — себя                                       |
| title      | генерирует CLI | `Complaint about <about>: <суть ≤60>` — по образцу текущего memory-complain.ts                                          |

### 3.3 Lifecycle

```
open ──(диспетчер триажирует: triage=*)──> resolved   (исход зафиксирован: инструкция/мутация/...)
open ──(диспетчер: необоснована)────────> rejected   (причина в resolution)
open ──(дубликат)───────────────────────> rejected   (related_to → каноническая, §3.4)
open ──(устарела: правило исчезло)──────> obsolete
```

- Переходы — только штатные (`ALLOWED_TRANSITIONS.open = [resolved, rejected, archived, answered]`), новых статусов не вводим.
- «Acknowledged»/«взял в работу» — поле `triage` (значения: `fix-instruction | steward-mutation | reject | need-info | duplicate | escalate`), статус остаётся `open` до исхода. Заполненный `triage` = сигнал «жалоба рассмотрена, диспетчер в курсе» для SLA-подсчёта и наблюдателя.
- Кто переводит: статус жалобы переводит только уровень **строго выше** target'а (§6). Воркер — никогда.
- Рецидив (та же суть после `resolved`): `resolved → open` запрещён переходами — создаётся новая жалоба с relation `based_on` на закрытую; рецидив сам по себе повышает приоритет триажа (corroborations наследуются упоминанием в rule).

### 3.4 Дедупликация

Одна суть = одна каноническая жалоба, независимо от авторов и прогонов.
Механика на стороне диспетчера (триаж, §5.3): при рассмотрении список открытых
и недавних закрытых по тому же `about` (`wolf list --type complaint` +
`wolf search "<суть>"`); совпадение сути `rule` (не дословный текст) —
дубликат: новый объект → `rejected`, relation `related_to` → каноническая,
у канонической `corroborations++` (через `wolf update`, §7-код). Корроборация
(3 воркера независимо) — повышенный приоритет: диспетчер обязан рассмотреть
такую жалобу в текущей приёмке, не откладывая.

## 4. Воркер (подача)

### 4.1 Критерии триггера («плохое правило» — проверяемые, не вокабуляр)

Триггер срабатывает, когда систематически плохо **правило** (playbook,
методика, рамка, правило памяти) — не тул и не разовый случай:

- **W1 Противоречие**: предписание методики противоречит предписанию брифа —
  следование обоим невозможно.
- **W2 Непокрытие**: случай подзадачи не покрыт методикой (нет применимого
  пункта).
- **W3 Вредность**: следование предписанию даёт неверный результат (пруф:
  красный тест / падение / числа).
- **W4 Двусмысленность**: предписание допускает два прочтения с разными
  результатами (второе прочтение — в evidence).

Не жалоба (делинеация §8): сбой тула → FRICTION; разовое отступление →
«Отклонения»; конфликт брифа с запретами безопасности → отказ, отклонение,
жалоба на диспетчера только если систематично.

### 4.2 Механика внесения

```bash
wolf complain \
  --about worker-implementer \
  --rule "п.2 МЕТОДИКИ playbook v2 требует X" \
  --evidence "дословная цитата + что произошло (файл/тест/числа)" \
  --proposal "уточнить X до Y / добавить исключение Z"
```

CLI создаёт объект `complaint` со статусом `open`, relation
`complain → about`, hot-signal Стюарду (существующий `appendComplaintSignal`).
Триггер в playbook'ах остаётся **процедурным и обязательным** (урок T15.2:
декларативное правило не срабатывает): критерии W1–W4 + команда + «строка в
Отклонениях жалобу НЕ заменяет».

### 4.3 Указатель в отчёте

Отдельная строка в отчёте воркера (после FRICTION, вне «Отклонений»):

```
COMPLAINT: <id> — <about>: <суть одной строкой>
```

Отчёт воркера не содержит тела жалобы — только указатель (решение владельца:
ответ не зашумлён, память — запрашиваемый канал).

### 4.4 Запрещённые действия воркера

- **Не мутатор**: не мутирует playbook'и и свою жалобу после подачи (поля
  rule/evidence/proposal неприкосновенны; опечатку исправляет новая жалоба).
- **Не диспетчер**: не переводит статусы (ни чужих жалоб, ни своей), не
  триажит, **не вызывает Стюарда** (в v1 вызывал — изменение D4).
- **Не ждёт реакции**: задача завершается штатно; реакция диспетчера приедет
  в следующем брифе/промпте, если диспетчер решит.
- **Не спамит**: одна жалоба = одна суть; повторная подача той же сути тем же
  автором запрещена (дубликаты сливает диспетчер, §3.4).

## 5. Диспетчер (executor-lead, L1)

### 5.1 Стоящее обязательство

При приёмке **каждого** отчёта воркера — до вердикта по задаче:

```bash
wolf list --type complaint --status open
```

Список пуст → обычная приёмка. Непустой → триаж каждой жалобы (§5.3) до или
параллельно с вердиктом; вердикт по задаче не блокируется жалобой (жалоба —
про правило, не про задачу). Обязанность стоит и когда отчётов с COMPLAINT-строками
нет: жалоба живёт в памяти, а не в отчёте (потеря отчёта не теряет жалобу).

### 5.2 Мандат и конфликт интересов

Диспетчер триажирует жалобы на цели **ниже своего уровня**: воркеры, их
playbook'и, скиллы, шаблоны. Жалобы с `about` уровня ≥ своего
(`executor-lead`, `mr-wolf`, `steward`) — диспетчер статус не трогает и в
триаж не вступает → эскалация (§6).

### 5.3 Дерево решений (по каждой открытой жалобе)

| #   | Состояние                                                    | Действие диспетчера                                                                                                                  |
| --- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Дубликат открытой/недавней закрытой (§3.4)                   | дубликат → `rejected` + `related_to` + `corroborations++` канонической; корроборация ≥3 → рассмотреть сейчас, не откладывать         |
| 2   | Не хватает данных (rule/evidence невоспроизводимы)           | статус `open`, `triage=need-info`; конкретный вопрос автору — в следующем брифе/промпте; `dispatch_ages++` (SLA тикает)              |
| 3   | Обоснована, чинится инструкцией (правило ок, разовый случай) | уточнение в следующий бриф/промпт автору; `resolved`, `resolution="инструкция: <что> → <куда>"`                                      |
| 4   | Обоснована, правило систематически плохое                    | `triage=steward-mutation` → вложенный вызов Стюарда (§5.5); после его отчёта `resolved`, `resolution="мутация: <supersede-пара id>"` |
| 5   | Необоснована (правило ок, автор ошибся)                      | `rejected`, `resolution="<причина>"`; разъяснение автору — в следующем промпте                                                       |
| 6   | Правило-цель исчезло (playbook superseded и т.п.)            | `obsolete`                                                                                                                           |

Фиксация исхода — обязательная пара: `wolf transition <id> <status>` +
`wolf update <id> --set resolution="..."` (краткая запись: решение, ссылки на
бриф/мутацию). Переход статуса без `resolution` — нарушение контракта;
координатор при приёмке отчёта lead'а это видит (suppression-гейт, §6.3).

### 5.4 SLA («не терять»)

Жалоба без исхода живёт не дольше **N=3 приёмок** (предложение; настройка —
Q3): приёмка создания (возраст 0) + максимум 2 откладывания. При каждой
приёмке, где открытая жалоба осталась open после триажа (ветви 2),
диспетчер делает `dispatch_ages++`; при `dispatch_ages ≥ 2` — обязательная
эскалация координатору: строка `ESCALATION: <id> — stalled` в отчёте lead'а +
тег `stalled` на объекте. Дальнейшую судьбу решает координатор (снять
диспетчера с жалобы, взять себе, вернуть с конкретным поручением).

### 5.5 Вызов Стюарда (ветвь 4)

- Диспетчер: `opencode run --agent steward` (единственный путь к мутациям —
  без изменений), вход = **id жалобы** (+ при необходимости контекст задачи).
  Стюард сам читает объект (`wolf get <id>`), сам выбирает лицо по существу
  (Наставник), сам мутирует: новая версия playbook полным содержимым →
  `wolf supersede <old> <new>`, авторство `steward:<лицо>`, одна жалоба —
  одна мутация (инварианты Наставника без изменений).
- Разделение ролей: **решает мутацию Стюард** (что и как менять), **исполняет
  логистику диспетчер** (вызов, контроль отчёта Стюарда, перевод статуса
  жалобы, резолюция). Диспетчер не диктует содержание мутации — он передаёт
  жалобу и принимает результат.

## 6. Эскалация (жалоба на самого диспетчера и выше)

### 6.1 Механика подъёма

Жалоба на `executor-lead` подаётся воркером штатно (§4.2, `about=executor-lead`)
— никаких особых путей: единый формат есть инвариант решения владельца.
Отличие только в судьбе:

| target                | Кто триажирует/переводит статус | Путь к адресу                                                                                                                                                                  |
| --------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| воркер/скилл/playbook | executor-lead (L1)              | штатный триаж §5.3                                                                                                                                                             |
| executor-lead         | mr-wolf (L0)                    | lead видит её в `wolf list`, НЕ трогает статус → строка `ESCALATION: <id> — жалоба на диспетчера` в отчёте lead'а координатору; координатор триажирует сам (то же дерево §5.3) |
| mr-wolf               | владелец                        | координатор видит при приёмке эскалаций → передаёт владельцу (вопрос в сессии/issue); сам себя не судит                                                                        |
| steward               | владелец                        | тот же путь, что и для mr-wolf (Стюард вне лестницы L0–L2; правило «уровень строго выше» — владелец)                                                                           |

Правило-инвариант: **статус жалобы переводит только уровень строго выше
target'а**. Подавец никогда не переводит статус своей жалобы; диспетчер не
судит жалобы на себя (конфликт интересов) — это и есть защита от
диспетчерского подавления.

### 6.2 SLA-эскалация

Жалобы `stalled` (§5.4) эскалируются тем же каналом: строка в отчёте lead'а →
координатор обязан отреагировать при приёмке отчёта (правило приёмки:
отчёт с ESCALATION-строкой не принимается без вердикта по каждой).

### 6.3 Защита от подавления (наблюдаемость)

- Всякий переход статуса требует непустой `resolution` (§5.3) — «тихое
  отклонение» невозможно.
- `rejected`-жалобы с корроборациями (повторная подача той же сути) видны при
  триаже (§3.4 рецидив) — подавленная жалоба возвращается.
- Координатор при приёмке любого отчёта lead'а видит эскалации; игнорирование
  ESCALATION-строки — нарушения контракта lead'а.

## 7. Изменения в контенте и коде (перечень, без самих правок)

### 7.1 Шаблоны (templates/base/)

| Файл                                                    | Суть правки                                                                                                                                                                                                                          |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `playbooks/complaint-protocol.md`                       | v2, обе стороны: подача объектом (wolf complain --rule/--evidence/--proposal, статус open), указатель COMPLAINT в отчёте; триаж диспетчера (дерево §5.3), SLA, эскалация; УБРАТЬ прямой вызов Стюарда воркером (D4); инварианты §4.4 |
| `playbooks/worker-implementer-playbook.md`              | ТРИГГЕР ЖАЛОБЫ → критерии W1–W4; команда wolf complain с новыми полями; строка COMPLAINT в ФОРМАТЕ ОТЧЁТА; «Стюарда не вызываю — решает диспетчер»                                                                                   |
| `playbooks/worker-researcher-playbook.md`               | то же, триггер от методики findings                                                                                                                                                                                                  |
| `playbooks/worker-reviewer-playbook.md`                 | то же, триггер от мандата/зон обзора                                                                                                                                                                                                 |
| `agents/executor-lead.md`                               | раздел «Жалобный контур» → «Триаж жалоб»: стоящее обязательство (запрос §5.1), мандат/конфликт интересов, дерево, фиксация исхода, SLA, правило эскалации; ESCALATION-строки в контракте отчёта                                      |
| `agents/mr-wolf.md`                                     | раздел «Жалобный контур» → приём эскалаций (жалобы на L1 — триаж координатором; на L0/Стюарда — владельцу), правило «отчёт с ESCALATION не принимается без вердикта»                                                                 |
| `agents/worker-implementer.md` (и researcher, reviewer) | строку про жалобный протокол привести к v2: указание на playbook, без деталей (детали — в playbook)                                                                                                                                  |
| `commands/complain.md`                                  | /complain владельца: wolf complain с новыми полями; триаж владельцем или передача координатору; вложенный Стюард остаётся путём владельца/координатора                                                                               |

### 7.2 Код (src/, после аппрува — в этой спеке не делается)

| Файл                                           | Суть изменения                                                                                                                                                                                                                |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/domain/memory-types.ts`                   | декларация типа `complaint` (§3.1) — единственная правка таксономии                                                                                                                                                           |
| `src/adapters/cli/commands/memory-complain.ts` | опции `--rule/--evidence/--proposal` (required), тип `complaint` вместо `observation`, title-генерация как сейчас; `--text` — deprecated-alias на `--evidence`; relation + hot-signal без изменений                           |
| `src/adapters/cli/commands/memory-update.ts`   | НОВАЯ CLI-команда `wolf update <id> --set k=v` поверх store.update; whitelist полей триажа (`triage, resolution, dispatch_ages, corroborations` + tags) — диспетчер не может править rule/evidence/proposal чужой жалобы (Q5) |
| тесты (unit/integration)                       | декларация типа; complain создаёт complaint/open с required-полями; update whitelist; статусные переходы жалобы                                                                                                               |

Существующее переиспользуется без изменений: ListFilters (type+status),
`wolf list`, `wolf transition`, `wolf supersede`, `wolf relation`,
appendComplaintSignal, recordRelation, вложенный вызов Стюарда.

## 8. Делинеация: жалоба vs FRICTION vs Отклонение

| Признак       | Жалоба (complaint)                                               | FRICTION                                                                                                                   | Отклонение                                                                     |
| ------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| Что не так    | правило/методика/шаблон систематически плохие (W1–W4)            | тул-вызов падает (сбой инструмента)                                                                                        | сознательное отступление от подзадачи в конкретной задаче                      |
| Канал         | `wolf complain` → объект `complaint` в памяти                    | строка `FRICTION: <n>× <тул> — причина; обход` в отчёте                                                                    | раздел «Отклонения» отчёта                                                     |
| Адресат       | диспетчер (триаж) → Стюард (мутация)                             | lead-агрегация; 2+ воркеров на один тул — системная, эскалация в отчёте                                                    | ревьюер/диспетчер при приёмке этой задачи                                      |
| Долговечность | память (запрашиваемая, переживает отчёты)                        | отчёт (сессия)                                                                                                             | отчёт (сессия)                                                                 |
| Конверсия     | —                                                                | повторяющаяся FRICTION: диспетчер решает — жалоба (если чинится контекстом: инструкция/плейбук) или блокер (если код тула) | повторяемость отступления → диспетчер подаёт жалобу на правило от имени данных |
| Пример        | «п.2 МЕТОДИКИ требует читать файл после правки — сломало X» (W3) | «rtk 3× не allowlist'ит wolf — обошёл через bash»                                                                          | «пропустил e2e: не было ключа провайдера»                                      |

Пограничное правило: тул падает **из-за плохой инструкции о нём** (allowlist в
промпте неверен) — это жалоба (правило), не FRICTION (тул).

## 9. План верификации (догфудинг)

Ручные живые прогоны в sandbox-проекте с отрендеренным набором (прецедент —
фаза C base-sets; детерминированные пруфы, без LLM-ключа где возможно).

| #   | Сценарий                         | Шаги и пруфы успеха                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| V1  | Автономный старт (главный)       | В seeded playbook worker-implementer посев заведомо плохого правила (конфликт с брифом, W1). Живая сессия воркера получает НОРМАЛЬНУЮ подзадачу без упоминания жалоб. Успех: воркер сам исполняет триггер → `wolf complain` (объект complaint/open с четырьмя полями) → строка COMPLAINT в отчёте → задача завершена штатно. Далее живая lead-сессия: приёмка → `wolf list --type complaint --status open` → триаж → вложенный Стюард → supersede-пара `steward:наставник` → `resolved` + resolution. **Контур замкнут без команды сверху.** Метрика автономности L2 (риск T15.2): 2 последовательных прогона с автономным стартом (после процедурного триггера v2); при 0/2 — контур признаётся нерастворимым инструкцией и эскалируется владельцу (кандидат: call-injection по тегу complaint) |
| V2  | Дедуп/спам                       | Три жалобы одной сути от трёх воркеров (разные формулировки). Успех: 1 каноническая (`corroborations=3`), 2 × `rejected` + `related_to`; в приёмке — одна строка исхода                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| V3  | Эскалация на диспетчера          | Воркер подаёт about=executor-lead. Успех: lead статус не тронул, в отчёте lead'а ESCALATION-строка; координатор триажирует (переход + resolution)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| V4  | SLA                              | Диспетчер дважды откладывает (need-info). Успех: `dispatch_ages=2` → третья приёмка: ESCALATION stalled в отчёте + тег на объекте                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| V5  | Делинеация (негативный контроль) | Воркер с тула-сбоем. Успех: FRICTION-строка в отчёте, объектов complaint не создано; повторяющаяся FRICTION конвертирована диспетчером осознанно                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| V6  | Подавление (негативный контроль) | Координатор при приёмке видит transition без resolution / ESCALATION без вердикта. Успех: контрактные правила рамок это запрещают и ловят при приёмке                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

Критерий приёмки реализации: V1–V6 подтверждены живыми прогонами с пруфами
(id объектов, переходы, supersede-пары) + `npm run check` зелёный + полное E2E
(правило проекта).

## 10. OPEN QUESTIONS (к владельцу)

1. **Q1 — тип объекта**: подтвердить новый core-тип `complaint` (рекомендую:
   required-поля enforce'ятся, точечный lifecycle и запрос) — или остаться на
   `observation` (меньше правок кода, но без валидации обязательности и с
   зашумлённым запросом)?
2. **Q2 — «acknowledged»**: подтвердить представление «взял в работу» полем
   `triage` без нового статуса (рекомендую: ноль правок governance) — или
   ввести статус `acknowledged` в MemoryStatus/ALLOWED_TRANSITIONS (явная
   семантика в списке, но правка schema + governance + transition CLI)?
3. **Q3 — SLA-параметр**: подтвердить N=3 приёмки (эскалация после 2
   откладываний). Альтернативы: N=2 (жёстче к диспетчеру) / N=5 (мягче).
4. **Q4 — централизация вызова Стюарда**: подтвердить изъятие прямого вызова
   Стюарда из обязанностей воркера (в v1 воркер вызывал сам; в v2 — только
   диспетчер/владелец). Это упрощает L2, но добавляет задержку: мутация
   случается не раньше приёмки.
5. **Q5 — `wolf update`**: подтвердить whitelist-режим (только поля триажа
   `triage/resolution/dispatch_ages/corroborations` + tags) вместо общего
   update любых полей (рекомендую: жалоба воркера неприкосновенна для
   диспетчера).
6. **Q6 — жалобы на Стюарда/владельца**: подтвердить путь «владелец, без
   нового механизма» (about=steward/mr-wolf → эскалация до владельца). Если
   жалобы на Стюарда станут регулярными — отдельный механизм (не в v2).

## 11. НЕ-цели (out of scope)

- Реализация кода/контента в этой спеке (спека — на ревью и аппрув).
- Автоматические мутации без Стюарда; авто-судейство жалоб (слепой судья,
  консилиум) — нет в v2.
- Санкции воркерам за спам жалоб (только дедуп + rejected с причиной;
  частотные лимиты не заводим).
- Изменение FRICTION-контура отчётов (он остаётся как есть; определена только
  граница §8).
- SLA по календарному времени (только по приёмкам).
- Миграция существующих observation-жалоб (sandbox-догфудинг) — начинаем с
  чистого списка.
- Автоматический поиск «похожих жалоб» семантикой (FTS-поиск остаивается
  диспетчером вручную; обученный матчинг — не v2).
- Web GUI, MCP-слой, распределение жалоб по тредам (жалоба живёт в shared).
