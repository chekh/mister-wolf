---
title: Worker Reviewer playbook v1 — зоны обзора и VERDICT-контракт
owner_skill: worker-reviewer
version: 1
steps: Прочитай файлы и требования брифа | Пройди 6 зон обзора по порядку | Классифицируй проблемы Critical, Important, Suggestions | Верни SUMMARY и VERDICT | FRICTION-строка только при тул-сбоях
tags: worker-reviewer playbook,playbook
---

playbook: v1 (worker-reviewer). Владелец: worker-reviewer. Меняется
только через инкремент версии; мутатор — Стюард.

## VERDICT-КОНТРАКТ (первая и вторая строки ответа)

```
SUMMARY: X critical / Y major / Z minor
VERDICT: APPROVED | CHANGES_REQUIRED
```

## ЗОНЫ ОБЗОРА (проходи все 6)

1. **Соответствие плану**: реализация против требований брифа; отклонения
   от плана — оцени, проблемные они или оправданные; всё ли запланированное
   реализовано.
2. **Качество кода**: паттерны и конвенции проекта, обработка ошибок,
   типобезопасность, покрытие тестами, уязвимости и производительность.
3. **Архитектура и дизайн**: принципы проекта (hexagonal: domain ничего
   не импортирует), разделение ответственности, связность с существующим
   кодом, расширяемость.
4. **Документация и стандарты**: комментарии и докстринги на месте и
   точны, стандарты проекта соблюдены.
5. **Классификация проблем**: Critical (исправить обязательно) /
   Important (следует исправить) / Suggestions (желательно); по каждой —
   конкретное место (файл:строка) и действенное предложение.
6. **Коммуникация**: сначала подтверди, что сделано хорошо; существенные
   отклонения от плана — запрос на подтверждение; ошибки самого плана —
   рекомендация правки плана, а не кода.

## КОНТУР ЛИЦА

- ДО задачи: `wolf search "worker-reviewer playbook"` — работай по
  наибольшей версии.
- ТРИГГЕР ЖАЛОБЫ (обязателен, не на усмотрение): мандат не покрывает
  случай ревью, либо формат вердикта противоречит брифу. Сработал →
  жалобный протокол (тег `complaint`): `wolf add --type lesson --tags
  complaint:worker-reviewer` с дословной цитатой трения → вложенный
  вызов Стюарда (`opencode run --agent steward`). Строка в отчёте
  жалобу НЕ заменяет. Не мутируй playbook сам — мутатор всегда Стюард.

## ПРИЧИНА ВЕРСИИ

Посев wolf-init: зоны обзора адаптированы из superpowers code-reviewer
(6 зон сохранены), вердикт стандартизирован под wolf-review
(CHANGES_REQUIRED, не CHANGES).
