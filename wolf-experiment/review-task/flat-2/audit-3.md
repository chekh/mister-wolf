# Аудит №3 — spec-v2.md (REVIEW-001, flat-2, итерация 3, финальная)

Финальный проход по трём перспективам (sec/cmp/con): остаточные уточнения,
регрессионная проверка правок итераций 1–2, сверка контрольных чисел и
терминологии. As-built — `code-ref/supply/`.

| ID | Severity | Перспектива | Раздел | Проблема | Предложение |
|---|---|---|---|---|---|
| A3-01 | minor | con | §3.3 (правка итерации 1) | Регрессия правки итерации 1: «timestamps … уникальны (верифицируется при загрузке ряда)» — as-built загрузчик (`load_anchor_universe`) сверяет счётчик якорей и `ts_keys == ts_bars[rows]`, но не уникальность всех timestamps; фактическая верификация — распределением дельт по всему ряду (урок L4, WП-0). | Уточнить механизм: уникальность подтверждается распределением дельт (метод L4), а сверка keys↔bars — при загрузке. |
| A3-02 | minor | cmp | §3.4 | §3.4 задаёт семантику «проход по барам t+1…t+h» для каждой пары (k, a); реализационный принцип «один проход до h_max, исходы для h < h_max — срезы потребителя» живёт только в §8 — читатель §3.4 может строить пер-ячеистые прогоны, что §8 запрещает. | Добавить в §3.4 скобку: «(реализация — один проход до h_max по §8; исходы для h < h_max — срезы потребителя)». |
| A3-03 | minor | con | §8 (инварианты модуля) | Правка итерации 1 добавила в контракт `mae_full_window_bps[dir,h]`, но список «Инварианты модуля (тест шага 1)» не дополнен: нет ни знакового условия (≤ 0), ни условия валидности (h ≤ window_len, маска V_h потребителя). | Дополнить инварианты модуля строкой для `mae_full_window_bps`. |
| A3-04 | minor | con | §8 (WП-2) | «бутстреп A/MBB §5» читается как два разных метода; метод A — это ресэмплинг исходов, реализуемый алгоритмом MBB (§5.1). | «бутстреп метода A (алгоритм MBB) §5». |
| A3-05 | minor | cmp | §5.2 | «Эпизоды/inter-arrival: … бутстреп вторично» — метод бутстрепа эпизодов не назван (метод A ресэмплит якорные исходы и к эпизодам неприменим; для окон в §5.3 указан «блочный бутстреп исходного ряда»). | Указать: блочный бутстреп исходного ряда (как §5.3), с публикацией пересечений границ блоков (уже есть). |
| A3-06 | minor | con | шапка | Спека прошла три итерации саморевью (v1–v3), но происхождение правок не отражено в шапке — трассируемость «v0 → v3» держится только на внешних audit-файлах. | Добавить строку в шапку: техническая errata саморевью flat-2 (audit-1/2/3.md); концептуальные определения не менялись. |
| A3-07 | minor | sec | §5.1 (числа) | Позитивный контроль после правок: 1 876 188 − 368 462 = 1 507 726 ✓; якоря 1 507 711 = бары 0…1 507 710 ✓; n_valid: bar_id ≤ 1 497 645 → 1 497 646 ✓; ceil(1507711/ℓ) = 150/75/38 ✓; floor = 149/74/37, согласовано с §5.5 «≈ 37 nonoverlap-equivalent» ✓; M1-пары A3 = 1 870 709 ✓ (CONTROL). Расхождений не найдено. | Правка не требуется (запись результата сверки). |
| A3-08 | minor | con | §3.5 (правка итерации 1) | Регрессионная проверка: добавленное перечисление тегов направлений и `group_id` не конфликтует с Прил. A (`dir: long|short`) и §3.12 — противоречий не внесено; формулировка «(as-built соответствие слоёв)» читается корректно. | Правка не требуется (запись результата сверки). |
| A3-09 | minor | cmp | §9 | «Критерии готовности» не упоминают новый выход контракта `mae_full_window_bps` (C4 no-hits) — критерий (4) «описать распределения MAE до target и на no-hit путях» теперь опирается на расширенный контракт; прямая ссылка на источник данных не критична, но повышает проверяемость. | Дополнить критерий (4) ссылкой: «(no-hits — `mae_full_window_bps`, §8)». |
| A3-10 | minor | sec | §3.6 | При непустом n_raw, но нулевом знаменателе после исключений (denom = n_raw − n_excluded = 0) поведение долей трёх метрик не оговорено (as-built: NaN вычислительно; публикация — null по конвенции §4.1(5), добавленной итерацией 1). Явной ссылки в §3.6 нет. | Добавить в §3.6: «при пустом знаменателе (все исключены) доли — null (конвенция §4.1(5))». |

SUMMARY: 0 critical / 0 major / 10 minor
