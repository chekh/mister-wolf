# Аудит №2 — spec-v1.md (REVIEW-001, flat-2, итерация 2)

Повторный полный аудит по трём перспективам (sec/cmp/con) с учётом правок
итерации 1; as-built — `code-ref/supply/`. Крупные противоречия сняты в
итерации 1; здесь — остаточные расхождения, недоопределённые термины и
уточнения протокола inference.

| ID | Severity | Перспектива | Раздел | Проблема | Предложение |
|---|---|---|---|---|---|
| A2-01 | major | con | §3.8 vs §4.1 (range_B) | Дубли с расхождением: §3.8 относит `B_only` к «балансам-отношениям» («публикуются B_min/B_max, а не точка»), а §4.1 range_B публикует «B_only/B_marginal (U=0) — точечный max−min». Only-доли строятся из hit-маргиналов и от порядка направлений не зависят (U=0) — bounds для B_only бессмысленны. | Уточнить §3.8: bounds — только для B_first (зависит от UNRESOLVED_LONG_SHORT); B_only/B_marginal — точечные. |
| A2-02 | minor | con | §6.5, Прил. A vs §3.13 | ATR(H4) упомянут в §6.5 («ATR(H1)/ATR(H4) — отдельные metric_id») и в примерах координат metric_id (`.atr_h4`), но потребитель ATR(H4) в v4.2 отсутствует: пороги §3.13 — только ATR(H1) (конвенция B0). Наследие v4 без потребителя. | Пометить в §6.5: ATR(H4) — наследие v4, в v4.2 не используется. |
| A2-03 | minor | cmp | §5.3 | «sensitivity к thinning» — thinning не определён (что прореживается и с каким шагом); критерий невоспроизводим между исполнителями. | Уточнить: прореживание якорей; сетка шагов фиксируется до расчётов (WП-3). |
| A2-04 | minor | con | §3.12(3) | «нога-служба < k» — нестандартный термин (по-видимому, опечатка от «амплитуда ноги < k»), в остальной спеке не встречается. | Заменить на «амплитуда ноги < k». |
| A2-05 | minor | cmp | §3.13 | Конвенция B0: «ATR(H1) закрытого H1-слота, предшествующего бару» — границы H1-слота и критерий «закрытости» в v4.2 не определены (наследие v4). | Определить: H1-слот — часовая решётка UTC; слот закрыт, если его последний наблюдаемый бар строго раньше текущего. |
| A2-06 | minor | con | §4.1 | Столбцы зон названы «Нижняя/Переходная/Верхняя», а в предикатных строках публикуются значения «LOW | MID | HIGH» — соответствие словарей явно не проговорено. | Добавить: LOW ≡ нижняя, MID ≡ переходная, HIGH ≡ верхняя зона. |
| A2-07 | minor | sec | §4 (манифест, ii) | «n_valid(h=10080) = 1 497 646 (подтверждено формулой §3.2 `min(h_max, oos_cut−1−bar_id)`…)» — формула описывает window_len отдельного якоря, а не счётчик; проверяемая формула счётчика не названа. | Уточнить: n_valid — число якорей с `oos_cut−1−bar_id ≥ 10 080`. |
| A2-08 | minor | cmp | §4.1 (dir_ratio) | `dir_ratio = max(P(L),P(S))/P(either)` и co-статистика `both_share = P(L∩S)/P(L∪S)` при P(either) = 0 — деление на ноль; явной отсылки к правилу отношений (5) в строке нет. | Добавить: P(either) = 0 → inconclusive по правилу отношений (5). |
| A2-09 | minor | con | Changelog rev.4→5, §3.2 | «10080 … ≈ 7–9 торговых дней» (changelog) vs канон §3.2 «7 непрерывных 24-часовых торговых суток ≈ 9–10 календарных дней» — расхождение исторической записи с каноном. | Без правки: changelog — журнал истории, не переписывается; канон — §3.2. Зафиксировано в аудите. |
| A2-10 | minor | sec | §5.1 | ГСЧ бутстрепа не зафиксирован: seed=42 есть, но алгоритм/библиотека и версия окружения не упомянуты — воспроизводимость реплик между окружениями не гарантирована. | Зафиксировать: генератор (библиотека/алгоритм) и версия — в манифесте прогона. |
| A2-11 | minor | con | §3.4 | «`pre_touch_mfe_bps`, `pre_touch_mae_bps` (signed, ≤ 0)» — знак ≤ 0 относится только к MAE; MFE ≥ 0 (as-built клип 0 снизу). Список читается двусмысленно. | «`pre_touch_mfe_bps` (≥ 0), `pre_touch_mae_bps` (signed, ≤ 0)». |
| A2-12 | minor | cmp | §5.1 | Не оговорено, пересчитывается ли вся статистика (в т.ч. терцильные границы §3.9) внутри каждой реплики по её маскам или фиксируется по исходной выборке — бутстреп-распределения различаются. | Указать: вся статистика, включая терцильные границы, вычисляется заново внутри реплики. |
| A2-13 | minor | con | §3.5 | «contiguous_run_count (число максимальных серий подряд идущих anchor row_index)» — корявая формулировка («число максимальных серий»); «максимальность» — свойство прогона, а не счётчика. | «число максимальных прогонов подряд идущих anchor row_index». |
| A2-14 | minor | con | §4.1 (max_slice_top10) | Tie-break топ-K — «лексикографический по более раннему extremum timestamp, затем episode_definition_id»; as-built — «меньший episode_index» (в рамках одного определения эквивалентен порядку extremum timestamps); соответствие не зарегистрировано. | Пометить эквивалентность as-built правила. |
| A2-15 | minor | con | §3.3 (правка итерации 1) | Регрессионная проверка правок итерации 1: добавленное предусловие уникальности timestamps с формулировкой «верифицируется при загрузке ряда» — as-built загрузка сверяет счётчик и ts_keys==ts_bars; уникальность фактически подтверждается распределением дельт (урок L4), а не загрузчиком. | Уточнить механизм верификации в итерации 3 (сослаться на L4-метод). |

SUMMARY: 0 critical / 1 major / 14 minor
