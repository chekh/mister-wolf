# Round 1 — перспектива: защищённость (executor-reviewer-security)

Объект: `spec-v0.md`. Вывод ревьюера сохранён дословно.

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| S1 | major | §3.2 («Вход», «Касание»), §3.4, §3.11, §3.13, §4 (манифест) | Отсутствует обязательная валидация качества OHLC real-ряда до расчётов: NaN в `high/low` даёт `False` во всех сравнениях барьеров §3.4 и FSM-подтверждениях §3.11 → молчаливый пропуск касаний/разворотов (partition-инварианты §4 это не ловят — разбиение остаётся полным); `close[t]=0`/отрицательные цены → NaN/±∞ в bps-барьерах и деление на ноль в `signed_gap` bps к `close[t−1]` (§3.13); `entry=0` ломает `(x−entry)/entry·10⁴`. sha256 в манифесте фиксирует неизменность файла, не валидность содержимого; §6.6 лишь признаёт ограничение без проверки | Дополнить манифест прогона (§4(i)) пре-флайт-сканом real-ряда scope: NaN/±∞ и значения ≤ 0 в OHLC/close → стоп до расчёта; «0 нарушений» зафиксировать контрольным числом манифеста |
| S2 | major | §3.4 (`order_direction`) | Для `order_direction` не выписано механическое правило через hit-маски и нет обязательных sentinel-тестов (четыре случая и свойство «sentinel не проходит сравнение» есть только для `order_tp_adv`): прямое сравнение «первых tp-баров двух направлений» при sentinel −1 даёт ложный LONG_FIRST/SHORT_FIRST; Direction partition §4 полноту сохраняет и порчу не ловит — молчаливое искажение C1 | По образцу `order_tp_adv`: явный блок `L_hit/S_hit = 0 < t ≤ h`, классификация только hit-масками и совпадением баров; обязательные тесты (−1,−1)→NEITHER, (t>0,−1)→FIRST, (−1,t>0)→FIRST, равные положительные→UNRESOLVED_LONG_SHORT |
| S3 | major | §5.1 (метод A) | Состав `Y_t = {order_tp_adv, order_direction, mae-поля, …}` не фиксирует h-зависимость классов (§8: классы — производные потребителя по h): не указано, что внутри реплики доли для каждого h пересчитываются из t-полей либо Y содержит классы per h; риск молчаливого бутстрепа h-специфичных долей (например, TP_FIRST при h=360) по классам одного h (h_max) → смещённые CI без признака ошибки | Зафиксировать в §5.1: в `Y_t` входят `t_tp/t_adv` (или классы для каждого h); h-зависимые доли обязаны пересчитываться внутри каждой реплики; тест на одной реплике — совпадение бутстреп-точки с прямой долей по h |
| S4 | major | §3.3, §3.5, §3.6 | Не зарегистрирован инвариант строгой монотонности/уникальности timestamp в едином ряду scope: при нулевых дельтах (дубликатах, возможных и в «отсортированном» ряду) `touch_id = timestamp[touch_row]` молча сливает касания разных баров в одну touch group, джойны «по timestamp» (§3.3) размножают строки; «согласованный позиционный маппинг» не определён — рассинхрон индексов при разных фильтрах; WП-0 считал распределение дельт, но вердикт «дубликатов нет» не зафиксирован как инвариант/контрольное число | Добавить обязательный тест WП-1 и контрольное число манифеста: timestamps строго возрастают, нулевых дельт нет; зафиксировать биекцию timestamp ↔ row_index как единственный канонический ключ соединения |
| S5 | major | §3.7, §3.8, §3.9, §3.10, §4.1(5) | Поведение при нулевом знаменателе не определено для долей/балансов: `share_of_hits_heavy` при \|H_resolved\|=0, `eventual_target_after_adverse` при \|A\|=0, `dir_ratio` при P(either)=0, `B_only` при onlyL+onlyS=0, `B_touch_group_direction` при 0 групп: «NaN к публикации запрещён», но обязательный выход не задан (образец есть только для `episodes_per_year` §4.1) — реализации молча разойдутся (исключение vs 0.0 vs null), «отношения — ненулевой знаменатель» §4.1(5) не определяет словарный статус такой ячейки | Распространить правило нулевого знаменателя по образцу `episodes_per_year` на все доли/балансы §3.7–§3.10: `estimate=null`, `support_status=insufficient`, DT-зона `inconclusive` |
| S6 | minor | §3.2 (`mae_before_fp`, `terminal_bps`) | NaN-семантика полей объявлена (NaN без касания / при truncated), но запрет чтения без маски (установленный для touch-полей §3.4/§8) на них не распространён; при будущем потреблении NaN протечёт в квантили/агрегаты молча | Дополнить §3.2 правилом потребителя: `mae_before_fp` читается только при tp-хите, `terminal_bps` — только при не-truncated окне (иначе null) |
| S7 | minor | §3.13 | Для ATR-кратностей не определено поведение при отсутствии закрытого H1-слота, предшествующего бару (начало ряда), и при ATR(H1)=0 (нулевой диапазон слота): деление на ноль / undefined метрику без флага | По образцу §3.6: отсутствие слота → исключение строки с публикацией доли; ATR=0 → null + отдельный счётчик (не 0/∞) |
| S8 | minor | §4.1, §5.1 | Метод квантильной интерполяции (q90/q95/q99, перцентильные CI реплик) не зафиксирован: linear/nearest/иные схемы дают расхождения на малых n — несводимость результатов между реализациями и прогонами при формальном «детерминизме соблюдён» | Зарегистрировать канонический метод квантилей (например, numpy «linear») в манифесте прогона рядом с версиями numba/numpy/polars |
| S9 | minor | Прил. A, §4 (инварианты публикации) | «NaN в parquet запрещён; отсутствие значения = null» (Прил. A) не включён в тестируемые инварианты публикации §4 — правило существует без валидатора, нарушение пройдёт молча | Дополнить инварианты публикации тестом: во всех float-колонках артефактов NaN отсутствует (отсутствие значения — только null) |

SUMMARY: 0 critical / 5 major / 4 minor
