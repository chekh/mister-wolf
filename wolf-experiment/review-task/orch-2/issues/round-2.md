# Round 2 — перспектива: полнота (executor-reviewer-completeness)

Объект: `spec-v1.md`. Вывод ревьюера сохранён дословно.

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| C1 | critical | §8 (контракт модуля first-passage) + §4 (C4), §9(4) | C4 требует «MAE на своём h среди no-hits», но контракт модуля §8 поля `mae/t_mae` на полном окне h не выводит, а WП-2+ запрещено создавать собственные реализации first-passage; источник (корпусные `mae_{dir,h}` §3.2 as-built) явно не указан, и не зафиксировано, по какому определению строится множество no-hit (M1-точный `t_hit` модуля или корпусной `fp_tau` с D1-гранулярностью при τ>7200 — множества различаются). Контракт фиксируется «до кодинга шага 1 WП-1», доработка после материализации ограничена правилом побитовой сверки | До кодинга шага 1 дополнить выход модуля §8 полями `mae_{dir,h}`/`t_mae_{dir,h}` полного окна (или явно зафиксировать чтение из корпуса c1_v4 + правило согласования no-hit с M1-точным `t_hit`) |
| C2 | major | §6.5, Прил. A | ATR(H4) упомянута («ATR(H1)/ATR(H4) — отдельные metric_id», координата `.atr_h4`), но операционного определения нет: H4-слоты, пороги-кратности, правило отсутствия закрытого H4-слота, поведение при ATR=0 не определены; enum `gap_threshold` (`bps22…q99`) значений `atr_h4` не содержит — упомянутая сущность без operational definition | Добавить определение ATR(H4) по образцу ATR(H1) (§3.13, конвенция B0) и соответствующие значения `gap_threshold`, либо внести ATR(H4) в §7 «Отложено» с критерием возврата |
| C3 | major | §3.9, §4.1 | `share_of_hits_heavy` и `eventual_target_after_adverse` прямо названы «метриками» (§3.9), но собственных строк DT не имеют; по §0 самостоятельная метрика ⟺ строка в DT, иначе нужна явная привязка co-статистики к обслуживаемой строке — такой привязки для обеих величин нет (терцильная строка §4.1 обслуживает только терцильные доли) | Указать в §3.9/C5 явную привязку (например, «co-статистики строки "терцильные доли §3.9"» по образцу `both_share` → `dir_ratio`) или добавить для них строки DT с зонами до расчётов |
| C4 | major | §0, §4 | Правило ценности §0 («метрика ⟺ строка DT; co-статистика ⟺ явная привязка») не подкреплено каталогом соответствия: для ряда публикуемых величин (P(L)/P(S)/P(L∩S)/none; coverage-варианты C2b; `same_touch_survival`/`renewed_horizon_availability` при δ≠60; `B_only`/`B_marginal`/`B_touch_group_direction`; inter-arrival/амплитуда/длительность/overshoot C2a; gap-таблицы кроме `top_year_share`; поля групп §3.5) обслуживающая строка DT или co-статистическая привязка не указаны — правило непроверяемо механически | Добавить в Прил. A таблицу соответствия «метрика → строка DT / co-статистика какой строки» (заполняется по §0 для всех публикуемых величин каталогов c1–c7) |
| C5 | major | §5 п.2–3 | Единица ресэмплинга для C2a/C2b/C2c не операционализирована: «бутстреп вторично» (эпизоды) и «блочный бутстреп исходного ряда» (окна) не определяют, какой ряд блокируется (ценовой / эпизодный ledger / ряд якорей), как обрабатываются окна/эпизоды/якоря на стыках блоков, каковы параметры thinning и какие статистики выводятся из реплик; запрет пересборки пути сформулирован только для метода A (п.1) | Дополнить п.2–3 §5: зафиксировать объект ресэмплинга и правила стыков блоков (по аналогии с п.1: маски/ledger перемещаются с объектами, ценовые блоки не склеиваются), указать хотя бы один параметр thinning |
| C6 | major | §6.1–6.2 | Контроли ссылаются на объекты v4 без их определения в спеке при заявленной самодостаточности (шапка «все определения… приведены здесь»): перечень «32 always-on target-or-terminal политик» и состав «инструментальных контролей v4» не приведены — повторение контролей и проверка 0/32 не воспроизводимы по тексту | Включить в §6 перечни (32 политики с параметрами; список инструментальных контролей) или точные ссылки на артефакты v4 с идентификаторами (файл/commit), делающие контроли исполнимыми |
| C7 | minor | §3.4 (граничные правила) | «adv ровно на последнем баре окна у no-hit → ADV_FIRST с пометкой» — пометка не определена: имя флага/поля, место в контракте модуля или каталоге C5 не указаны | Ввести именованный флаг (например `adv_on_last_bar_no_hit`) в контракт модуля §8 / публикацию C5 |
| C8 | minor | §3.11, §4.1 (`episodes_per_year`) | Календарная привязка эпизода для счёта по годам не указана (год по extremum, confirm или start): для `active days` supply указано «по дате extremum», для `episodes/year` — нет; эпизоды, пересекающие границу года, распределяются неоднозначно | Зафиксировать в §3.11: год/месяц эпизода для годовых счётов = календарный год extremum (согласовано с active days supply) |
| C9 | minor | §3.12(3) | Состояние матча `unresolved_episode` не имеет явного определения (матч к эпизоду с флагом `episode_order_unresolved`? попадание touch_row в INIT_UNRESOLVED-лид-ин?), при этом стоит первым в детерминированном приоритете причин — состав состояния реализация выберет произвольно | Определить явно в §3.12(3), какие кандидаты порождают `unresolved_episode` (отдельно от `init_or_censored`) |
| C10 | minor | §3.13 | Пороги событийности не привязаны к величине (применяются к `signed_gap` или \|gap\|?) и не определено множество для квантильных порогов q95/q99 (по каким переходам считаются квантили — все соседние пары баров scope? глобально или по-годово?); от этого зависит состав «gap-событий» в C7/DT `top_year_share` | Указать: порог применяется к абсолютной величине \|signed_gap\|; квантили q95/q99 — по всем соседним парам наблюдаемых баров scope, глобально (метод интерполяции уже в манифесте §4(iii)) |
| C11 | minor | §5.1 vs §3.8 | Правило нулевого знаменателя §3.8 адресует только публикацию; поведение бутстреп-реплики при пустом знаменателе ячейки внутри реплики (возможно для мелких ячеек, напр. k=240, h=10080) не определено — NaN в распределении реплик? пропуск реплики? — реализации разойдутся молча | Распространить правило §3.8 на реплики: статистика реплики = null при нулевом знаменателе, CI не публикуется, `support_status = insufficient` |
| C12 | minor | §4.1 (`top_year_share`) | Не указано, входят ли частичные годы 2021ᵖ/2025ᵖ в числитель/знаменатель `top_year_share` (для `episodes_per_year` ограничение полными годами задано явно); при включении частичных лет доля топ-года смещена | Оговорить состав лет (например, только полные 2022–2024; частичные годы — отдельной пометкой) |
| C13 | minor | §3.2, §3.11, §3.13, §4.1 (C2b) | Календарная конвенция не зафиксирована: таймзона/границы «наблюдаемых торговых суток» (знаменатель `active_days_share`), границы ISO-недель и месяцев для дат anchor/extremum/confirm, «календарные Sat/Sun» для `crosses_weekend` — единого фрейма (UTC?) нет | Зафиксировать в §3.2 единый календарный фрейм (например UTC) для всех календарных привязок раунда |
| C14 | minor | §3.11 (Face-validity QA) | «Разметка человеком по инструкции WП-1» — инструкция как артефакт не существует, её создатель и содержание (критерии swing'а ≥ k для разметчика) не определены; открытый вопрос §10(1) покрывает только критерии отбора фрагментов | Добавить «инструкция разметки» в выходы WП-1 (с исполнителем и содержанием: правила подсчёта swing'ов, обработка неоднозначных баров) |
| C15 | minor | §4.1 (`unresolved_decision_sensitive`) | Термин «поддержанная строка» не определён (достаточно ли `support_status = sufficient`, или учитываются `exploratory_tail`-строки) — вердикт метастроки зависит от этой трактовки | Определить явно: «поддержанная» = `support_status = sufficient` (иные статусы исключаются из обеих зон) |
| C16 | minor | §8 (WП-6) | «Framing-патч старых отчётов» — перечень целевых отчётов (B1–B5? b0–b5?) и объём правок не определены; состав «старых отчётов» в спеке не приведён | Перечислить целевые отчёты в строке WП-6 (или дать ссылку на перечень в артефактах v4) |

SUMMARY: 1 critical / 5 major / 10 minor
