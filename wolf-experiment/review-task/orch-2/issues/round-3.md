# Round 3 — перспектива: консистентность (executor-reviewer-consistency)

Объект: `spec-v2.md` + сверка с `code-ref/`. Вывод ревьюера сохранён дословно.

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| K1 | major | Шапка (строки 3–6) vs Changelog (строки 20–74) | Статус заявлен как «rev.5.3.1 **final** (rev.5.3 + техническая errata **e1–e4**…); Концептуальное ревью закрыто; правки — только технической errata», но ниже в том же документе — два более поздних блока errata: e5–e13 (security, раунд 1) и c1–c16 (completeness, раунд 2, включая **critical** c1). Ревизия/статус не отражает собственный объём правок документа. | Поднять ревизию (напр. rev.5.3.3) и в строке статуса перечислить все действующие errata-блоки (e1–e13, c1–c16), оставив e1–e4 как историческую ссылку. |
| K2 | major | §8, строки 1174–1178 vs code-ref `supply/first_passage.py:351–398, 612–616` | Контракт модуля (дополненный critical-erratum c1) требует в выходе `mae_{dir,h}`/`t_mae_{dir,h}` для каждого h ∈ {360, 1440, 10080}; фактический выход модуля (`FpArrays` / `_fp_kernel`) содержит только tau/path-поля и `window_len` — полей mae по горизонтам нет нигде в code-ref. Источник для «MAE на своём h среди no-hits» (C4) отсутствует, а WП-2+ запрещено строить собственные реализации. | Либо реализовать поля в модуле до WП-4, либо зарегистрировать в §8 явный статус «не реализовано в шаге 1, отдельная задача до C4» с указанием источника. |
| K3 | minor | §8 строка 1171, §3.4 строки 423–428 vs code-ref `supply/first_passage.py:356–358`, `supply/fp_consumer.py:20–23` | `pre_touch_window_empty[dir,k]` заявлена как «обязательная маска» в выходе модуля и артефакте; в as-built `FpArrays` поля нет — код прямо документирует «потребитель выводит как `tau == 1`». | Зарегистрировать в §8 конвенцию: маска материализуется в артефакте при записи (колонка = `tau == 1`) либо является производной потребителя с обязательным тестом эквивалентности. |
| K4 | major | Прил. A, строки 993–998 vs code-ref `supply/catalog.py:68–72, 94–97, 100–117` | Прил. A: `horizon` — nullable «при неприменимости к grain строки» (глобальные строки, напр. `top_year_share`); в коде `horizon` входит в `_NOT_NULL` (с комментарием «verbatim контракта Прил. A»), обязателен в `build_record` и not-null в схеме. Аналогично `grain` not-null в коде, но отсутствует в not-null списке Прил. A. | Синхронизировать: либо исключить `horizon` (и `grain`) из `_NOT_NULL` в коде, либо убрать их из nullable-фразы Прил. A и включить в not-null список. |
| K5 | major | §3.8, строки 558–564 (правило e9: `estimate = null`, «не 0.0, не NaN», «реализации не расходятся молча») vs code-ref `supply/wp2_cells.py:340–350, 391–394, 479`, `supply/delay.py:24, 92–104`, `supply/matching.py:420–432` | Зарегистрированное правило нулевого знаменателя нарушено в implementations двумя разными способами: `wp2_cells` возвращает **0.0** («NaN нигде (нулевые знаменатели → 0.0)» — неотличимо от содержательного нуля при публикации), а `delay`/`matching` — **NaN**. Только терцили имеют оговорку о null на уровне записи каталога. | Ввести единую конвенцию на уровне калькуляторов (передавать признак пустого знаменателя / None) и зафиксировать её в §3.8/§4 как проверяемый инвариант публикации. |
| K6 | major | §3.11, строки 640–649 (INIT: «сегмент от БОЛЕЕ РАННЕГО экстремума») и 727–733 (sensitivity-range «глобальные конвенции») vs code-ref `supply/episodes.py:427–430` vs `483–486` | В TRACKING код определяет `high_first` как «high раньше low» (подтверждение валидно только для UP-сегментов, ext=high). В INIT та же конвенция разрешается противоположно: `high_first → exit_up=True` (сегмент UP от lo). При «high раньше low» более ранний экстремум — hi, и по правилу §3.11 сегмент должен быть DOWN; в коде — UP. Две точки разрешения одной «глобальной конвенции» в одном модуле взаимно инвертированы. | Зафиксировать в §3.11 явное правило разрешения INIT-неоднозначности для sensitivity-конвенций (по аналогии с «от более раннего экстремума») и привести код к единой семантике. |
| K7 | minor | §5.1, строки 1076–1077 | «drawn ≈ 150/75/37 реплик-блоков»: при N = 1 507 711 и ℓ = 40 320 `drawn = ceil(N/ℓ) =` **38** (150 и 75 корректны); 37 — это `nonoverlap_equivalent = floor(N/ℓ)` (см. §5.5, строка 1109). | Исправить на «150/75/38» (или «drawn 150/75/38, nonoverlap 149/74/37»). |
| K8 | minor | §3.8 строка 534 vs §3.9 строка 578 | Одна и та же величина `|B|/|H_resolved|` названа `share_resolved` (§3.8) и `share_of_hits_heavy` (§3.9), при этом §3.9 заявляет «третьих имён нет». Нарушение принципа §0 «одно понятие — один термин». | Добавить в §3.8 явное отождествление: «share_resolved ≡ share_of_hits_heavy (§3.9) — точка по разрешённым». |
| K9 | minor | §3.2 строки 289–290, §3.4 строки 350–355 vs code-ref `atlas/rollout.py:67–68, 190–191, 214–215, 248–251, 263–267, 391–392` | As-built ссылки на ядро корпуса не находятся в code-ref: функции `_path_fp_tau_zone` и константы `Z_FP_M1_MIN` нет нигде в снапшоте; фактические ядра — `atlas/rollout.py:_excursions_kernel` и `supply/first_passage.py:_fp_kernel`. Заявленная «Int32-материализация `np.where(isinf, −1)` в сборке корпуса» не наблюдаема: rollout хранит fp-времена как float64 со значением **+inf** при отсутствии (List(Float64) `fp_up_min`/`fp_dn_min`), а `t_mfe/t_mae` за пределами окна — NaN, не −1. | Уточнить в §3.2/§3.4 точные имена файлов/функций code-ref (или пометить конвенции Int32/−1 как верифицированные WП-0 по внешнему артефакту с указанием его идентификатора в манифест). |
| K10 | minor | §3.2 строка 287, §8 строка 1174 vs code-ref `supply/first_passage.py:47–48, 279, 322`, `supply/wp2_cells.py:172, 186`, `atlas/config.py:36, 267–269` | §3.2 «window_len (внутр. k_total) = min(H_max, бары до OOS-границы)» не различает два окна: корпусный `window_len` с капом **86 400** (60 торговых дней) и рабочее окно v4.2 `k_tot = min(10 080, window_len)`; фактическое имя колонки артефакта — `k_tot` (не `window_len`/`k_total`). При чтении H_max как 10 080 формула §3.2 даёт k_tot, а не корпусный window_len, который верифицировал WП-0. | Зарегистрировать в §3.2 обе величины явно: корпусный `window_len = min(86 400, oos_cut−1−bar_id)` и рабочий `k_tot = min(10 080, window_len)` с именем колонки артефакта `k_tot`. |
| K11 | minor | §4.1(5) строка 934 vs строка 960 (DT «терцильные доли») | Правило (5): «терцили — `|A_i| ≥ 30` **каждого**» (все три терциля); строка DT: «`|A_i| ≥ 30` **обоих**» (только Q1/Q3). Разные множества терцилей для одного критерия поддержки. | Унифицировать формулировку в обоих местах (напр.: «каждого терциля, участвующего в сравнении Q1/Q3»). |
| K12 | minor | §3.8 строки 528–529 vs §3.11 строка 722 и code-ref `supply/catalog.py:53–56` | §3.8 использует нотацию `P_pessimistic`/`P_optimistic`, тогда как §3.11 и as-built словарь `DICT_V42` («слова pessimistic/optimistic запрещены») запрещают эту терминологию. Одна концепция границ — две терминологии. | Переименовать в §3.8 в нейтральную нотацию (`P_lb`/`P_ub` или `.lb`/`.incl_unresolved` — как в зарегистрированных суффиксах кода). |
| K13 | minor | Прил. A строки 978–979 vs code-ref `supply/catalog.py:139` | Зарегистрированное множество grain: `k` \| `k,h` \| `k,h,dir` \| `k,h,a,dir` \| … \| `global`; пример в docstring `build_record` — `"k,a,dir"` (без h) — отсутствует в enum. | Заменить пример в коде на `"k,h,a,dir"` (или расширить enum Прил. A, если grain без h действительно допустим). |
| K14 | minor | §4(ii) строка 860 vs code-ref `supply/first_passage.py:66` | n_valid(h=10080) = 1 497 646 «подтверждено… и **A6** WП-0» — в коде то же контрольное число документировано как «(**A4** WП-0)»; номер пункта аудита WП-0 расходится. | Сверить номер пункта аудита с `wp0_audit_results.json` и выровнять (A4 или A6) в обоих местах. |

SUMMARY: 0 critical / 5 major / 9 minor
