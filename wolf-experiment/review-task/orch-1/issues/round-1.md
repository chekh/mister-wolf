# Round 1 — Security (executor-reviewer-security)

Спека: `spec-v0.md` (1043 строки). Вывод ревьюера сохранён дословно.

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| SEC-1 | major | §3.4, §8, Прил. A | Противоречие конвенций NaN: Прил. A запрещает NaN в Parquet («отсутствие значения = null»), но контракт модуля first-passage материализует в `fp_{run_id}.parquet` `pre_touch_mfe_bps = NaN`, `pre_touch_mae_bps = «0.0/NaN»` (выбор не зафиксирован), `terminal_bps = NaN`, `mae_before_fp = NaN`. Двойная конвенция «0.0/NaN в вычислительном массиве» не определена для артефакта: потребители WП-2+ могут тихо прочитать NaN/0.0 как содержательное значение, а побитовые сверки артефакта между реализациями нестабильны | Зафиксировать единственную материализацию (null) для всех parquet-артефактов, включая `fp_*`; распространить запрет NaN с каталогов c* на артефакт модуля; добавить тест «NaN отсутствует в артефакте» в инварианты шага 1 WП-1 |
| SEC-2 | major | §8 | Материализованный `fp_{run_id}.parquet` не имеет контроля целостности: sha256 артефакта не пишется в `run_manifest.json`, атомарность записи не специфицирована (нет tmp+rename+fsync). Частично записанный при краше файл (detached-режим, RSS-стоп, стоп 30 мин) будет молча потреблён WП-2+; «повторный расчёт — только со сверкой побитово» не определяет, с чем сверяется артефакт | Писать sha256 артефакта в манифест после записи и проверять при каждом чтении; запись только атомарно (tmp → os.replace); при несовпадении хэша — стоп до разбора, как для входного ряда |
| SEC-3 | major | §3.2 (OOS), §3.11, §8 | Отсутствует механический инвариант OOS-границы: вход модуля first-passage и FSM — весь `bars_1m_enriched.parquet`, включая хвост после 2025-08-04; единственный барьер — клип `window_len = min(H_max, бары до OOS-границы)` by-construction. Баг клипа (или чтение хвоста в touch_id/эпизодах) даст молчаливое загрязнение prospective holdout, который спека сама объявляет обязательным для финальной валидации. Текст «OOS: только бары train ∪ val» и «delayed_row не в OOS» не подкреплены тестом | Добавить обязательный тест шага 1 WП-1: для всех якорей `anchor_row + window_len < row(oos_cut)`; инвариант «ни одно поле fp_-артефакта не зависит от баров ≥ oos_cut»; тот же тест для FSM и gap-ledger |
| SEC-4 | major | §4.1, §5.1, §5.5 | Preregistration-дисциплина не обеспечена механикой: правила «зоны фиксированы до расчётов», «пороги хвостов могут быть изменены до расчётов, но не после просмотра», «тип бутстрепа не меняется после просмотра CI» не имеют аудируемого следа — пороги DT, пороги N ≥ 400/2000, выбор длины блока не включаются в `run_manifest.json` и не связываются с версией спеки. Post-hoc изменение зон неотличимо от исходного | Включать в каждый `run_manifest.json`: sha256 спеки, commit спеки и полные значения порогов/зон DT; изменение любого порога — только новой ревизией спеки с записью в Changelog; тест «пороги прогона совпадают с порогами зарегистрированной ревизии» |
| SEC-5 | major | §4, §8 (общие правила) | Каталоги c* пишутся в фиксированные пути без версии прогона в имени (`c1_supply.parquet` и т.д.) и без спецификации изоляции smoke-прогона: smoke-срез и полный прогон пишут по одному пути; перезапись затирает предыдущий прогон без следа, а смешение записей разных прогонов в одном файле возможно при неатомарной записи. Append-only журнала нет — потеря записей невосстановима | Smoke — только в изолированный путь (`*_smoke` / отдельный каталог); полный прогон — атомарная замена файла целиком; в манифесте фиксировать список записанных файлов с sha256 |
| SEC-6 | major | §3.7, §4.1 (`range_B`, балансы) | Не специфицировано поведение при нулевых знаменателях годовых балансов: `B_only = onlyL/(onlyL+onlyS)`, `B_marginal = P(L)/(P(L)+P(S))`, `B_first` при годе без единого hit (или без onlyL/onlyS) дают 0/0. Правило нулевого года введено только для `episodes_per_year`; NaN к публикации запрещён — реализация молча выберет исключение или null без stop-rule, расхождение между годами в `range_B` не определено | Распространить правило нулевого года на все балансы: пустой знаменатель → estimate=null, `support_status=insufficient`, зона `inconclusive`; явный тест «ни одна публикация не содержит 0/0-артефакт» |
| SEC-7 | minor | §4 (манифест), §8 | `run_id` попадает в имена файлов (`fp_{run_id}.parquet`) и связывает записи с манифестом, но формат/алфавит run_id не зафиксированы и не валидируются: при появлении пользовательских суффиксов — риск path traversal и коллизий имён артефактов | Зафиксировать алфавит run_id (UTC ISO + `[a-z0-9-]`), валидировать перед построением любого пути; запрет пользовательских строк в путях |
| SEC-8 | minor | §3.4, §4 (манифест), §8 | Побитовые кросс-чеки (`fp_tau` M1-зона, побитовая сверка артефактов) не оговорены как зависимые от окружения: версии numba/numpy/polars записываются в манифест постфактум, но нет пина версий и правила «побитовая сверка валидна только при совпадении окружения с эталоном WП-0» — при обновлении контейнера ULP-расхождения могут дать ложный стоп или, хуже, тихо пройденную непобитовую сверку | Пиновать версии в образе; в протоколе кросс-чека сравнивать окружение с эталонным; при несовпадении — сверка по допуску с явной пометкой, не побитовая |
| SEC-9 | minor | §3.13 | ATR(H1) «закрытого слота, предшествующего бару» (конвенция B0): поведение при отсутствии предшествующего закрытого H1-слота (дырки данных, начало ряда) не определено — молчаливый пропуск баров или NaN сместит знаменатели gap-таблиц и порогов `atr_h1_x1/x2` без следа | Определить null + флаг «ATR недоступен», публиковать долю баров без ATR; gap-таблицы по ATR-порогам — с явным n_raw только по обеспеченным барам |
| SEC-10 | minor | §3.11 (Face-validity QA) | Независимость разметчика Face-validity не обеспечена процедурно: нет требования слепой разметки (инструкция WП-1 не фиксирует запрет просмотра вывода кода до/во время разметки) — риск подгонки человеческого счёта под реализацию, ±1-допуск тогда теряет силу контроля | В протокол WП-1 включить слепой режим: разметка фрагментов до запуска кода на них, фиксация разметки (файл с хэшем) до сравнения; правки разметки после сравнения — только с записью причины |
| SEC-11 | minor | §8 (общие правила, п.3) | «Стоп при отсутствии прогресс-лога > 30 мин» не определяет механизм остановки: не указано, кто и как завершает процесс; зомби-процесс может продолжать писать артефакты после решения о стопе — гонка с последующим прогоном и неконсистентный манифест | Определить supervisor: автоматический kill + пометка `aborted` в `run_manifest.json` + недействительность частичных артефактов (удаление/суффикс `.partial`), запрет параллельных прогонов в один каталог |
| SEC-12 | minor | §4 (манифест, e1) | Два канонических контрольных числа расходятся без объяснения: строки real-ряда train∪val = 1 507 726 vs якоря корпуса = 1 507 711 (разница 15) — errata e1 фиксирует сами константы, но не причину расхождения; сведение двух независимых сверок в одну цепочку целостности невозможно без ожидаемой связи между ними | Записать в манифест ожидаемую причину разницы (например, последние бары без валидного окна ≥1) как третью проверку; любое иное расхождение строк/якорей — стоп до разбора |

SUMMARY: 0 critical / 6 major / 6 minor
