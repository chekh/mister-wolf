# Round 2 — Completeness (executor-reviewer-completeness)

Спека: `spec-v1.md` (1128 строк). Вывод ревьюера сохранён дословно.

Ревью по перспективе полноты завершено (спека прочитана полностью, 1128 строк). Замечания — только по полноте определений, покрытию случаев, разбиениям и ссылкам; безопасность и соответствие внешнему коду не рассматривались.

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| CMP-1 | major | §8 (контракт модуля), §3.4 | Значения выходных полей модуля first-passage при no-hit (t_tp = −1) не специфицированы: `mae_through_touch_bar_bps`, `touch_bar_adverse_bps`, `pre_touch_mfe_bps`, `pre_touch_mae_bps`, `t_pre_touch_mae`; маска `pre_touch_window_empty` определена только через «пусто ⟺ t_tp = 1», случай t_tp = −1 не покрыт — контракт невозможно зафиксировать полным до кодинга шага 1 WП-1 | Явно определить no-hit конвенцию: величины → null (NaN запрещён Прил. A), `t_pre_touch_mae` → −1/null, значение `pre_touch_window_empty` при no-hit — отдельно от «пустого hit-интервала», плюс обязательная hit-маска в артефакте |
| CMP-2 | major | §3.9, §0, §4.1 | `share_of_hits_heavy` и `eventual_target_after_adverse` — метрики гейта §3.9 («решение по обеим метрикам»), но не имеют строк/зон DT, что противоречит §0 (метрика входит в атлас ⟺ строка DT); решающее правило гейта не операционализировано, критерий готовности (6) невыполним по формальному правилу | Добавить DT-строки с зонами для обеих метрик либо зарегистрировать их как co-статистики строки терцильных долей; сформулировать решающее правило гейта §3.9 |
| CMP-3 | major | §4.1(5), строки `dir_ratio`, `tp_first_ratio` | Исход при нулевом знаменателе отношений не задан: `dir_ratio`/`both_share` при P(either)=0, `tp_first_ratio` при N(hit)=0; критерий «отношения — ненулевой знаменатель» объявлен, но последствие (null/insufficient/inconclusive) не определено — в отличие от детально прописанных случаев `range_B` (s6) и `episodes_per_year` | Распространить конвенцию s6 (null + `support_status=insufficient` + DT `inconclusive`) на все строки-отношения с зависимым знаменателем |
| CMP-4 | major | §3.12(3) | Категории разбиения `unresolved_episode` и `init_or_censored` определены только именами: не задано, что матч к эпизоду с `episode_order_unresolved` — это `unresolved_episode`, а касание в INIT/INIT_UNRESOLVE D-лид-ине или censored-хвосте — `init_or_censored`; от этого зависят детерминированный приоритет и partition-тест суммы долей | Дать строгие определения обеих категорий (какой флаг/состояние FSM какую категорию порождает) до включения в partition-тест §4 |
| CMP-5 | major | §3.13, §6.5, Прил. A | Построение H1-слотов (границы часа, таймзона, минимальная заполненность слота) и «конвенция B0» не определены в спеке; ATR(H4) упомянут (§6.5, координата `.atr_h4` в Прил. A) без определения, конвенции слотов и порогов — ATR-пороги C7 нереализуемы по внутренней спеке | Определить агрегацию M1→H1/H4 (выравнивание, полнота слота, что при частичном слоте) и раскрыть конвенцию B0, либо дать точную ссылку на определяющий документ/раздел |
| CMP-6 | major | §5.2, §5.3 | Бутстреп-протоколы C2a («бутстреп вторично») и C2b/C2c («блочный бутстреп исходного ряда») не операционализированы: не указан ресэмплируемый ряд и параметры блоков; «исходного ряда» читается как пересборка ценовых блоков — конфликт с запретом метода B в §5.1 | Указать явно: для C2b/C2c — ресэмплинг якорного ряда Z_t по методу A (или отдельное определение), для C2a — ряд эпизодов (по confirm timestamps) с длинами блоков; убрать неоднозначность «исходного ряда» |
| CMP-7 | major | §4.1 (терцильные доли) | Термин «attribution-sensitive» в определении MID не определён; при естественном чтении «интервалы перекрываются» LOW («перекрываются при достаточной поддержке») и MID пересекаются — разбиение зон не дизъюнктно, вердикт DT вычислим двумя способами | Определить attribution-sensitive независимо от перекрытия интервалов (например, доля U выше порога) и переформулировать зоны LOW/MID/HIGH как дизъюнктное разбиение |
| CMP-8 | major | §4.1 (правила чтения, `episodes_per_year`) | Правило сравнения с зонами задано только для bounds-величин (правило 2); для строки `episodes_per_year` счёт — sensitivity-range (явно «не bounds») — правило чтения диапазона (по какой границе диапазона сравнивать с порогами 1.5/3) отсутствует | Дописать правило: например, зона присваивается ⟺ обе границы sensitivity-range попадают в одну зону, иначе MID/inconclusive |
| CMP-9 | minor | §5.1 | Числа «drawn ≈ 150/75/37» не согласованы с собственной формулой: ceil(1 507 711/40 320) = 38, а не 37 (37 = floor = nonoverlap_equivalent, что верно для §5.5, но не для drawn) | Исправить на 150/75/38 или явно выписать оба ряда значений (drawn и nonoverlap-equivalent) |
| CMP-10 | minor | §3.4 (граничные правила) | «adv ровно на последнем баре окна у no-hit → ADV_FIRST с пометкой» — пометка не именована, не определено место её фиксации (поле/флаг/артефакт) | Ввести именованный флаг (например, `adv_at_window_edge`) и указать каталог/колонку публикации |
| CMP-11 | minor | §3.7 | `onlyL`/`onlyS` использованы в B_only без определения (выводимы из P(L), P(S), P(L∩S), но спека требует строгости для знаменателей) | Определить: onlyL = hit L ∧ ¬hit S, onlyS — зеркально, на валидных якорях ячейки (k,h) |
| CMP-12 | minor | §3.8, §3.9 | Конвенция принадлежности границ терцилей не задана: не определено, в какой терциль попадает t_adv, равный границе Q1/Q3 (влияет на разбиение A по терцилям и на клампинг U_i при Σ\|U_i\| = \|U\|) | Зафиксировать полуоткрытые интервалы и правило для равенства границе (клампинг включительно) |
| CMP-13 | minor | §4 (артефакты), Прил. A | Для C8 (Decision Table, «итог раунда») не определён артефакт: в списке перечислены c1…c7 + figures, имя/формат c8 отсутствует — итоговый результат WП-5 без места и схемы публикации | Добавить артеффикат c8 (имя файла, формат, схема колонок) в список §4 и Прил. A |
| CMP-14 | minor | Прил. A (Nullability) | `cost_model_id` присутствует в схеме записи §4, но не отнесён ни к not null, ни к nullable — разбиение полей по nullability неполно; допустимое значение в раунде без стоимостной модели не задано | Включить в разбиение и определить значение по умолчанию (например, `none`) |
| CMP-15 | minor | §4.1 (`unresolved_decision_sensitive`) | Термин «поддержанная строка» не определён (связь с `support_status = sufficient`? учитываются ли `exploratory_tail`?) | Определить: поддержанная = support_status ∈ {sufficient} (или явно включить/exclude exploratory_tail) |
| CMP-16 | minor | §4.1 (`active_days_share`) | Знаменатель «наблюдаемые торговые сутки года» не определён операционно (календарные сутки с ≥1 наблюдаемым M1-баром scope?) | Дать определение подсчёта суток (сутки с ≥1 баром train∪val в данном году) |
| CMP-17 | minor | §4.1 (`active_days_share`) | Множество месяцев для предиката «Σ пустых мес. ≥ 6» не задано: только полные 2022–2024 или включая частичные 2021ᵖ/2025ᵖ (от этого зависит максимум суммы) | Указать явно (например, только полные годы, максимум 36) |
| CMP-18 | minor | §4.1 (`max_slice_top10_coverage_share`) | Случай < 10 qualifying-эпизодов не оговорён: «топ-10» при меньшем числе эпизодов — доля по всем имеющимся? пометка? | Определить top-min(10, N_qualifying) и обязательную пометку/стоп-правило при N_qualifying < 10 |
| CMP-19 | minor | §4.1 (терцильные доли) | Порог «\|B\| ≥ 100» не специфицирован: суммарное \|B\| §3.10 или per-термильные \|B_i\| — проверка HIGH реализуется неоднозначно | Уточнить индекс (например, Σ\|B_i\| по обоим терцилям) |
| CMP-20 | minor | §3.13 | База квантилей q95/q99 как gap_threshold не определена: по какому множеству переходов и по какой величине (absolute gap? signed?) считаются квантили | Определить: по всем соседним наблюдаемым барам scope, от absolute gap, с фиксацией в metric_id |
| CMP-21 | minor | §3.9 | «Медианный remaining horizon» терцилей — remaining horizon не определён (предположительно h − t_adv, но конвенция не задана) | Определить явно (например, remaining = h − t_adv в баровах) |
| CMP-22 | minor | §3.6, §3.13 | Ряд величин «публикуется» без привязки к metric_id/артефакту: доля исключённых delayed-строк (§3.6), доля баров без ATR (§3.13) — catalog-схема их не покрывает | Завести именованные metric_id (например, `c2b.delay_excluded_share`, `c7.atr_unavailable_share`) в Прил. A |
| CMP-23 | minor | §3.8 | «Абсолютные доли обоих видов публикуются по (k, h, a, dir)» — для UNRESOLVED_LONG_SHORT координата a не участвует (§3.4: порядок направлений считается «k без a»), публикация по (k,h,a,dir) для него не определена | Уточнить: UNRESOLVED_LONG_SHORT — по (k,h,dir) с a_bps = null |
| CMP-24 | minor | §6.1, §6.2 | Перечень «32 always-on target-or-terminal политик», определения e_net и payoff rule, состав «инструментальных контролей v4 — повторно» не приведены и не привязаны к артефактам — при декларации самодостаточности ревизии контроли невоспроизводимы по спеке | Дать перечень/ссылку на конкретный артефакт или раздел v4 для каждого контроля |
| CMP-25 | minor | шапка («Самодостаточность»), §8(3), §3.2, §3.3 | Ссылки на внешние «уроки» (L4, L15, L22, L24) и «операционные требования v4 §6 (2026-08-12-…)» не резюмированы в спеке при заявке, что все определения приведены здесь | Резюмировать ключевые требования (L24, v4 §6) в спеке или явно ограничить декларацию самодостаточности |
| CMP-26 | minor | §3.11 (FSM) | Для заявленного «полного псевдокода» не задана инициализация hi/lo на первом баре INIT (формально выводима из пункта (1), но псевдокод неполон) | Добавить строку инициализации (первый бар: hi = high[0], lo = low[0] с их row) |
| CMP-27 | minor | §3.11 (QA) | Объём фрагментной выборки не задан: число фрагментов на каждый из 5 типов и суммарная длительность отсутствуют (критерии отбора — открытый вопрос §10.1, но минимальный объём не зарегистрирован) | Зафиксировать минимальный объём (N фрагментов на тип / суммарные бары) до запуска QA |
| CMP-28 | minor | §3.2, §4 (манифест, s4) | Порог M1-зоны кросс-чеков `Z_FP_M1_MIN` = 7200 назван, но не включён в preregistration-список манифеста (s4 перечисляет зоны DT, пороги хвостов, длины блоков) — место его фиксации не определено | Включить порог M1-зоны в `run_manifest.json` наравне с остальными порогами |

SUMMARY: 0 critical / 8 major / 20 minor
