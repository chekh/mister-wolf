# Round 3 — Consistency (executor-reviewer-consistency)

Спека: `spec-v2.md` (1246 строк); code-ref: `code-ref/atlas/`, `code-ref/supply/`. Вывод ревьюера сохранён дословно.

Сверка завершена: спека прочитана полностью (1246 строк), все 20 Python-модулей code-ref просмотрены, ключевые (first_passage, fp_consumer, touch_groups, delay, episodes, matching, wp2_cells, catalog, rollout, config, aggregate, gaps) — построчно. Итог ревью по перспективе CONSISTENCY:

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| CON-1 | major | §3.2, §3.4(e4), §8 | As-built утверждения о ядре корпуса неверифицируемы в code-ref: функция `_path_fp_tau_zone` (§3.4: «ровно как в ядре корпуса») не найдена ни в одном модуле; «Int32-материализация `np.where(isinf, −1)`» и «дневная (D1) решётка при τ > 7200» нигде не реализованы. Единственные fp-ядра в code-ref противоречат этим деталям: `atlas/rollout.py::_excursions_kernel` хранит `fp_up_min/fp_dn_min` как float64-минуты с sentinel **+inf** (не −1/Int32, без D1-снаппинга); `supply/first_passage.py::_fp_kernel` — int64/−1, но окно ≤ 10080 без D1-зон. Подтверждены кодом только bps-форма барьера и sentinel −1 (в supply) | Либо включить в code-ref модуль сборки корпуса (снаппинг D1 + Int32-материализация), либо снять из §3.2/e4 ссылки на внутренности `_path_fp_tau_zone`, оставив проверяемый контракт артефакта (sentinel/гранулярность по parquet) |
| CON-2 | major | §8 (контракт модуля), c1 | Контракт модуля first-passage расходится с реализацией (`supply/first_passage.py`, `wp2_cells.py`): (а) `t_adv[dir,k,a]` объявлен выходом контракта, но не материализуется — adverse-время выводится потребителем из `tau_dn/tau_up` противоположного направления на уровне a (docstring `fp_consumer.py`); (б) обязательная маска `pre_touch_window_empty[dir,k]` отсутствует в `FpArrays`/артефакте — выводится как `tau == 1`; (в) систематический дрейф имён: `t_tp`→`tau_up/dn_k{lvl}`, `mae_through_touch_bar_bps`→`mae_thru_*`, `pre_touch_mfe_bps`→`pre_mfe_*`, `t_pre_touch_mae`→`t_pre_*`, `touch_bar_adverse_bps`→`touch_adv_*`, `window_len`→колонка `k_tot`; (г) теги направлений `up/dn` вместо `long/short` (Прил. A `dir`). Спека декларирует контракт «фиксируется до кодинга шага 1 WП-1» | Привести §8 к фактическому интерфейсу артефакта (`fp_col_tag`, `k_tot`, выводимые потребителем поля) либо зарегистрировать отображение имён contract↔artifact отдельной таблицей в Прил. A |
| CON-3 | minor | §3.2 vs §8/§5.1 | «window_len» означает две разные величины: §3.2 — корпусную длину `min(H_max=86400, oos_cut−1−bar_id)` со скобкой «(внутр. k_total)», §8 — выход модуля, который в коде есть `k_tot = min(10080, window_len)` (колонка артефакта `k_tot`, `FpTimings.window_len`). В code-ref имя `k_total` не существует, а корпусная колонка называется как раз `window_len` — скобка §3.2 переворачивает фактическое именование; §4(ii) цитирует «формулу §3.2 `min(h_max, …)`», подменяя H_max на h_max (численно эквивалентно для h=10080, но ссылка неточна) | Развести имена: `window_len_corpus` vs `k_tot_v42`; исправить скобку «(внутр. k_total)» и ссылку §4(ii) |
| CON-4 | major | c14, Прил. A vs `supply/catalog.py` | Прямое противоречие errata c14: спека требует `cost_model_id = 'none'` **not null**; код пишет `cost_model_id: None` (null) в `build_record`, и `_NOT_NULL` не включает `cost_model_id` — валидатор null-пропускает, каталог публикует null в not-null поле | Записывать строку `"none"` и включить `cost_model_id` в `_NOT_NULL` |
| CON-5 | major | §4.1(5), c3, §3.7 vs `supply/wp2_cells.py` | Конвенция нулевого знаменателя: спека — `estimate = null`, `support_status = insufficient`, DT `inconclusive`, «при n = 0 доли не публикуются»; код — `_ratio`/`c1_cell`/`TercileStats` возвращают **0.0** при нулевых знаменателях («NaN нигде (нулевые знаменатели → 0.0)»), т.е. фабрикуют оценку; при этом `delay.py`/`matching.py` в том же пакете возвращают **NaN** — три разных конвенции. 0.0 неотличим от легитимного значения на уровне записи каталога; при `n_raw ≥ 30` и `|A| = 0` строка `eventual` может уйти в публикацию как `sufficient` с estimate 0.0 | Вернуть из калькуляторов NaN/None-сигнал (или флаг пустого знаменателя) и конвертировать в null+insufficient на уровне `build_record`; унифицировать конвенцию между wp2_cells/delay/matching |
| CON-6 | minor | s12, §4(ii) vs `supply/first_passage.py` | Объяснение инварианта «rows − anchors = бары ряда без валидного forward-окна… (последние бары без единого будущего бара)» не согласуется с кодом: `n_anchors = oos_cut − 15` (якоря = бары с `window_len ≥ 15`), «без единого будущего бара» — это 1 бар, а не 15; минимальное окно 15 баров нигде в спеке не зарегистрировано | Задокументировать в §3.2/§4 порог минимального окна (15 баров) как причину rows−anchors=15 |
| CON-7 | minor | Прил. A vs `supply/catalog.py` | Not-null набор кода расходится с Прил. A: код добавляет `horizon` и `grain` в `_NOT_NULL` (спека: `horizon` nullable при неприменимости к grain — строки per-k/global), а `cost_model_id` исключён (см. CON-4); `build_record` никогда не фильтрует `k_bps` по grain («k_bps не фильтруется») вопреки «k_bps… null при неприменимости» (строки grain=global) | Синхронизировать `_NOT_NULL` и фильтрацию ключей с Прил. A |
| CON-8 | minor | §3.12(5), §4.1 vs `supply/matching.py` | Признак qualifying для топ-K coverage: спека — свойство эпизода `amplitude_bps ≥ k`; `c2c_prototype` выводит qualifying из наличия matched_qualifying-групп («эпизод без matched-групп виден как nonqualifying — ограничение rev.1»). Числовая доля топ-K не меняется (нулевые вклады), но `n_qualifying_published`/пометка «<10» и подсчёт N_qualifying расходятся со спецификацией, при том что полный признак доступен в `EpisodeLeg.qualifying` | Использовать `EpisodeLeg.qualifying` (amplitude) в c2c-агрегации вместо вывода по матчам |
| CON-9 | minor | §4.1(5) vs §4.1 (строка «терцильные доли») | Порог поддержки терцилей сформулирован дважды по-разному: §4.1(5) «терцили — `|A_i| ≥ 30` **каждого**» (все три терциля), условие HIGH DT-строки — «`|A_i| ≥ 30` **обоих**» (только Q1 и Q3); расходятся на требование к Q2 | Зафиксировать одну формулировку (логично «обоих» Q1/Q3, т.к. сравниваются только они) и в §4.1(5) сослаться на неё |
| CON-10 | minor | §5.1 vs `atlas/aggregate.py` | Единственный реализованный в code-ref MBB — `atlas/aggregate.py::block_bootstrap_ci` — атлас v2: 500 реплик, `block_len=16`, sub-sample cap 20 000; это противоречит прегистрированному протоколу §5.1 (2000 реплик, seed=42, ℓ ∈ {10080, 20160, 40320}, полная вселенная N = 1 507 711, «никаких отбрасываний»). Модуль бутстрепа v4.2 в code-ref отсутствует — риск повторного использования v2-реализации для раунда v4.2 | Пометить `atlas/aggregate.py` как v2-наследие (не для v4.2) и реализовать §5.1 отдельным модулем с параметрами из манифеста |

SUMMARY: 0 critical / 4 major / 6 minor
