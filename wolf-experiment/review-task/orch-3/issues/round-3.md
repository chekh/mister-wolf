# Round 3 — Consistency review (spec-v2.md + code-ref/)

Код-референс каталога `atlas/` относится к другому атласу («атлас инструмента», спека v2 с §4.3/§5.6/§9.3 — стили, свопы, вердикты) и не содержит утверждений ревьюуемой спеки v4.2; все сверки выполнены по `supply/` (модуль first-passage, потребитель, delay, episodes, matching, touch_groups, wp2_cells, catalog). Все файлы supply/ прочитаны полностью, спека — полностью (1321 строк). Сверены: контрольные числа (CONTROL vs §4), sentinel-конвенции §3.2/§3.4, контракт §8, классификация/bounds/терцили, FSM §3.11, матчинг §3.12, каталог/Прил. A.

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| K-1 | major | Прил. A vs §3.13 (errata e29), §6.5 | Словарь ключевой колонки `gap_threshold` в Прил. A (стр. 1052): `bps22\|bps50\|bps75\|atr_h1_x1\|atr_h1_x2\|q95\|q99` — не содержит `atr_h4_x1\|atr_h4_x2`, введённых errata e29 в §3.13 (стр. 839: «значения словаря gap_threshold: atr_h4_x1 \| atr_h4_x2, координата metric_id .atr_h4»). Прил. A упоминает координату `.atr_h4` (стр. 1059), но не значения словаря — внутри одной ревизии два несовместимых словаря; C7/WП-4 не сможет записать ATR(H4)-строки по Прил. A. | Дополнить словарь `gap_threshold` в Прил. A значениями `atr_h4_x1\|atr_h4_x2`. |
| K-2 | major | §8, §3.2, §5.1(e39) vs `supply/first_passage.py` | Контракт модуля §8 (стр. 1237–1242, e23/e24) заявляет как входящие в выход: `mae_window_bps[dir]` / `mfe_window_bps[dir]` (running-экстремумы полного окна) и `terminal_bps[dir]`. В коде их нет: `FpArrays` (first_passage.py:351–398) содержит только 12 полей × уровни + `window_len`; ядро `_fp_kernel` делает ранний выход после взятия всех уровней, не сохраняя оконные экстремумы/close правого края; wp2_cells.py:6–10 прямо перечисляет потребителей (tau-тайминги, «pre-touch/mae — C4/WП-4»), window-поля и terminal не упомянуты. §3.2 маркирует контракт «as-built», e39 включает эти поля в `Y_t` бутстрепа — утверждение спеки о реализованном не соответствует снапшоту кода. | Либо реализовать поля в `_fp_kernel`/материализации, либо убрать as-built маркировку §8 для e23/e24 и пометить их как контракт следующей ревизии модуля (с записью в Changelog). |
| K-3 | major | §4.1(5), e14 vs `supply/wp2_cells.py` | Спека (стр. 997–1003, e14): при нулевом знаменателе `estimate = null`, `support_status = insufficient`; «замена нуля неявной константой запрещена». Код: `_ratio` (wp2_cells.py:340–350) возвращает 0.0 при знаменателе 0; `c1_cell` (стр. 296–300) — «shares … 0.0 при n_raw = 0»; `TercileStats.p/p_min/p_max` — «0.0 при нулевом знаменателе» (стр. 362–364). NaN-защита каталога (`build_record`) на 0.0 не сработает — пустой знаменатель публикуется как содержательный 0.0, нарушение e14/e27; docstring закрепляет это как контракт («NaN нигде (нулевые знаменатели → 0.0)»). | Возвращать `None` (или явный флаг пустого знаменателя) из калькуляторов; на уровне каталога гарантированно конвертировать по счётчикам (`a_count`, `h_resolved`, `n_raw`), пока это не сделано — расхождение с e14 остаётся. |
| K-4 | major | Прил. A vs `supply/catalog.py` | Спека (стр. 1075–1080): `horizon` — nullable «при неприменимости к grain строки», и перечень grain (стр. 1053–1054) содержит `k` и `global` (строки DT `episodes_per_year` per k, `top_year_share` global — без горизонта). Код: `_NOT_NULL` (catalog.py:68–72) требует `horizon` и `grain` всегда not null → строки per k / global невозможно записать с `horizon = null` (ValueError «not-null поля пусты»). Контракт записи кода и схема DT/Прил. A взаимно противоречивы. | Убрать `horizon` (и `grain`, если он не пересматривается в спеке) из `_NOT_NULL`, приведя к Прил. A; либо внести в спеку явное правило not-null для horizon и фиктивного значения для аглобальных строк. |
| K-5 | major | §4.1 vs `supply/matching.py` | DT-строка `max_slice_top10_coverage_share` (стр. 1038): «tie-break равных вкладов — лексикографический по более раннему extremum timestamp, затем episode_definition_id». Код: `c2c_prototype` (matching.py:384–387) сортирует по `(-unique_successful_anchors, episode_index)`, причём эпизоды упорядочены по `confirm_row` (episodes.py docstring, стр. 205: «в порядке confirm_row»), что не эквивалентно порядку по `extremum timestamp` — при равных вкладах код выберет другой топ-10, чем требует спека. | Заменить ключ сортировки на `(ext_ts_ns, episode_definition_id)` (`Episode.ext_ts_ns` уже вычисляется); либо зарегистрировать в спеке текущее правило как осознанное отклонение прототипа rev.1. |
| K-6 | minor | §3.4, §8 vs `supply/first_passage.py`, `supply/fp_consumer.py` | Спека: «маска `pre_touch_window_empty` обязательна в артефакте» (стр. 471); §8 перечисляет `t_adv[dir,k,a]` и `pre_touch_window_empty[dir,k]` в выходе модуля (стр. 1225–1234); e34 — «флаг `adv_at_window_edge` в fp-артефакте» (стр. 487). Код: эти поля не материализованы — `fp_consumer.py` (стр. 20–23) выводит их по тождествам (`t_adv = tau_противоположного[a]`, `pre_touch_window_empty ≡ (tau == 1)`); `adv_at_window_edge` не реализован нигде (выводим как `t_adv == h`). Функционально эквивалентно, но формулировка «в артефакте/в контракте» коду не соответствует. | Привести §8/§3.4 к фактической конвенции («производные потребителя по зарегистрированным тождествам»), либо материализовать `t_adv`, маску и флаг в fp-артефакте. |
| K-7 | minor | §4 (e10) vs `supply/first_passage.py` | e10 (стр. 113–115): «метка `completed` в манифесте только после успешного финала; WП-2+ читают только completed-артефакты». Код: гейт потребления `read_latest_fp` (first_passence.py:245–250) проверяет `status == "PASS"`. Значение статуса в спеке и коде различаются — буквальная реализация WП-2+ по спеке (искать `completed`) не распознает артефакты, валидированные кодом. | Унифицировать литеральное значение статуса (`completed` или `PASS`) в спеке e10 и в манифестах кода. |
| K-8 | minor | Changelog rev.4→5 vs §3.2 | Changelog (стр. 273–274): «10080 = 10 080 наблюдаемых торговых баров ≈ 7–9 торговых дней»; §3.2 (стр. 348): «10080 observed M1 bars = 7 непрерывных 24-часовых торговых суток ≈ 9–10 календарных дней». 10080/1440 = ровно 7 торговых суток; «7–9 торговых дней» противоречит §3.2 и арифметике (документ заявляет самодостаточность — все определения здесь). | Исправить в changelog на «7 торговых суток (≈ 9–10 календарных дней на 24/5)». |
| K-9 | minor | §5.1 (внутри) | Стр. 1129–1130: «ℓ ∈ {10080, 20160, 40320} → drawn ≈ 150/75/37 реплик-блоков» — при канонической формуле того же пункта `drawn_blocks_per_replicate = ceil(N/ℓ)` и N = 1 507 711: 40320 → ceil(37.39) = **38**, а не 37 (37 = floor, т.е. `nonoverlap_equivalent`, который в §5.5 использован корректно). Реализатор по формуле получит 38, и сверка с «≈37» разойдётся. | Исправить на «≈ 150/75/38» (или «37–38»), оставив 37 только для `nonoverlap_equivalent_blocks = floor(N/ℓ)`. |
| K-10 | minor | §4 манифест (iii) vs шапка | Стр. 915: «`spec_revision` (текущая — rev.5.3.2)» при статусе документа в шапке «rev.5.3.3 final» (стр. 3). Манифесты прогонов по этой спеке зафиксируют устаревшую ревизию — прямо подрывает цель e9 («артефакты разных ревизий спеки должны быть различимы по метаданным»). | Обновить константу в §4(iii) до rev.5.3.3 (или заменить на ссылку «текущая ревизия шапки», чтобы не дублировать). |
| K-11 | minor | §4 (C2b) vs Прил. A | C2b публикует «active weeks (ISO-неделя даты anchor)» (стр. 973), но словарь `denominator_unit` Прил. A (стр. 1071–1073): `anchors\|anchor_outcomes\|episodes\|windows\|touch_groups\|blocks\|gap_timestamps\|trading_days\|calendar_months` — единицы недель нет; долю активных недель записать в каталог без расширения словаря невозможно (дни/месяцы не годятся). | Добавить значение `calendar_weeks` в словарь `denominator_unit` (или переопределить метрику active weeks через существующие единицы). |

SUMMARY: 0 critical / 5 major / 6 minor

Пояснение к сверке (что подтверждено кодом, не вошло в замечания): контрольные числа §4 полностью совпадают с `CONTROL` (oos_cut 1 507 726, якоря 1 507 711 = oos_cut−15, n_valid 1 497 646 = oos_cut−10080, sha256 `3f24c40e…`, 1 870 709 M1-пар, OOS 2025-08-04); sentinel-конвенции (−1, маска `>0`, нормализация `t̃→∞`, hit-маски как единственный источник классов, инвариант e3) соответствуют `fp_consumer.py`; bps-форма барьеров (e4), pre-touch «tp-бар исключён», ранние экстремумы, знаковые конвенции — соответствуют `_fp_kernel`; FSM §3.11 (INIT/TRACKING порядок, INIT_UNRESOLVED, флаг, «одно подтверждение на бар», календарь по extremum, три счёта ledger) — соответствуют `episodes.py`; touch groups §3.5 и delay §3.6 (единый знаменатель, инварианты) — соответствуют коду; каталоги `supply/catalog.py` соответствуют §4/Прил. A по колонкам и словарям (кроме K-4).
