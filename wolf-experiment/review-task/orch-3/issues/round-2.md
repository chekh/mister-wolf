# Round 2 — Completeness review (spec-v1.md)

| ID | Severity | Раздел | Проблема | Предложение |
|---|---|---|---|---|
| C-1 | critical | §8 (контракт модуля), §3.4, §4 (C4), §9(4) | Каталог C4 требует «MAE на своём h среди no-hits» (критерий готовности §9(4) — «распределения MAE до target и на no-hit путях»), но контракт модуля first-passage §8 материализует только `mae_through_touch_bar_bps`, pre-touch и touch-поля; поля `mae/mfe/t_mae/t_mfe_{dir,h}` из §3.2 — лишь as-built контракт ядра, в контракт выхода не входят. При этом «один проход… отдельные прогоны запрещены» и «WП-2+ читают материализованный артефакт» — WП-4 не может вычислить заявленную метрику без нарушения контракта | Добавить в контракт модуля выход running-экстремумов окна (mae/mfe на h_max со срезами потребителя по h) либо явно указать допустимый источник (поля ядра) для MAE-h |
| C-2 | major | §3.7 | `B_only = onlyL/(onlyL+onlyS)` — величины `onlyL`/`onlyS` нигде в спеке не определены (числа/доли якорей только-long-hit и только-short-hit? на какой сетке (k,h)?) | Дать строгое определение: например `onlyL = #{t: 0 < t_hit_long ≤ h ∧ t_hit_short = −1}` с указанием ячейки |
| C-3 | major | §3.12(3) | Для состояний матча `direction_mismatch`, `init_or_censored`, `unresolved_episode`, `unmatched` формальные предикаты отнесения не записаны (строго определён только matched — п.1–2); дан лишь приоритет причин. Реализатор WП-1 выведет правила сам | Записать предикат каждого из шести состояний (например: нога противоположного направления содержит touch_row → direction_mismatch; touch_row в лид-ине INIT или censored-хвосте → init_or_censored; матч с episode_order_unresolved → unresolved_episode) |
| C-4 | major | §4.1(5) (errata e14) | «Единое правило нулевого знаменателя» перечисляет не все строки: `dir_ratio` (P(either)=0), `both_share`, `tp_first_ratio` (N(hit)=0), `surv60`, `mae_p90_over_k` (пустое множество hits — квантиль по пустому набору), `max_slice_top10_coverage_share`, `top_year_share` (0 gap-событий), `range_B` (0 полных лет) не покрыты | Распространить формулировку на все строки DT: «любая метрика с нулевым знаменателем или пустым множеством → estimate=null, insufficient, inconclusive» с явным добавлением перечисленных |
| C-5 | major | §4.1 (`unresolved_decision_sensitive`) | Метастрока определена через «все поддержанные bounds-строки», но перечень bounds-строк DT не зафиксирован (неясно, считаются ли bounds у `tp_first_ratio`, `mae_p90_over_k`, `surv60`, `dir_ratio` и т.д.) — вердикт метастроки зависит от неопределённого набора | Явно перечислить, какие из 11 строк DT являются bounds-строками (или дать критерий отнесения) |
| C-6 | major | §6.5, §3.13, Прил. A | ATR(H4) упомянут дважды («ATR(H1)/ATR(H4) — отдельные metric_id», координата `.atr_h4` в Прил. A), но нигде не операционализирован: построение H4-слотов не определено (e12 определяет только H1), пороги ATR(H4) отсутствуют, значения `atr_h4_*` в словаре `gap_threshold` нет | Либо операционализировать H4 (календарное правило слотов по образцу B0 + пороги + значения словаря), либо удалить упоминания ATR(H4) из §6.5 и Прил. A |
| C-7 | major | §6.1–6.2, §8 | Контроли §6 (отрицательный always-on 0/32, «инструментальные контроли v4 — повторно») не привязаны ни к одному пакету работ §8 (WП-1…WП-6 их не упоминают); операционализация e_net, payoff rule, cost floor и состава 32 политик в спеке отсутствует; `terminal_bps` (§3.2), необходимый target-or-terminal политикам, в контракт модуля §8 не входит | Указать исполняющий WП и точный источник определений (раздел v4) для каждого контроля; для terminal-политик включить `terminal_bps` в контракт модуля или указать источник |
| C-8 | major | §4 / Прил. A / §5.1 | Размещение MBB-результатов в схеме записи не специфицировано: формат строки имеет только `ci_lo/ci_hi`, а §5.1 требует публиковать раздельно `bootstrap_se`, `bootstrap_ci_lo/hi`, `mcse_of_*`; «MBB-CI — co-статистика» без указания metric_id/колонок/каталога; `denominator_unit=blocks` объявлен, но какие строки его используют — не сказано | Определить кодирование бутстреп-величин (суффиксы metric_id напр. `.bootstrap_se`, `.ci_lo`, либо отдельные поля/файл) и правила denominator_unit для таких строк |
| C-9 | major | §3.13 | База квантильных порогов `q95/q99` не определена: квантили какой величины (абс `signed_gap`? `adverse_long/short`?) и по какому множеству переходов (весь train ∪ val?); также не зафиксировано определение самого «gap-события» для порогов — по \|signed_gap\| или по направленной компоненте | Указать величину и скоуп расчёта квантилей + явное определение события (например, `abs(signed_gap) ≥ q95`) |
| C-10 | major | §4.1 (`active_days_share`), §3.11 (C2a), §4 (C7) | Правило обращения с частичными годами 2021ᵖ/2025ᵖ для календарных метрик не зафиксировано: предикаты `any(год < 0.50)` / Σ пустых месяцев — по каким годам считаются; «мин по 2022–24» в колонке агрегации не расшифрован; топ-1 год/квартал концентрации (C2a) и `top_year_share` (C7) — включаются ли частичные годы | Зафиксировать единое правило (только полные 2022–2024 либо частичные с пометкой) и определить смысл «мин по 2022–24» |
| C-11 | minor | §3.2 vs §3.4 | `mae_before_fp` (§3.2) и `mae_through_touch_bar_bps` (§3.4) — внешне одна и та же величина, но соотношение/алиасность не определены; `mae_before_fp` далее нигде не используется и в контракт §8 не входит | Пометить `mae_before_fp` как as-built имя поля ядра (синоним) или удалить из §3.2 |
| C-12 | minor | §4 (словари `dict_v4_2`) | Словарь значений `n_eff_status` не перечислен: упомянуто только `not_estimated`; значение при «числовой n_eff отдельной обоснованной процедурой» не задано, хотя поле not null | Добавить полный перечень значений `n_eff_status` в словари dict_v4_2 |
| C-13 | minor | §3.9 | «Медианный remaining horizon» терцильных строк не определён (h − t_adv? по якорям терциля A_i или всех?) | Дать формулу (например, median(h − t_adv) по A_i) |
| C-14 | minor | §3.11 (TRACKING) | «…episode_order_unresolved = true (в основной счёт не входит, учитывается в bounds ниже)» — ниже в §3.11 bounds отсутствуют (слово запрещено; введены ledger-счёта + sensitivity-range): dangling ссылка внутри раздела | Заменить «в bounds ниже» на «в ledger-счётах flagged_episode_count / count_if_flagged_included и sensitivity-range» |
| C-15 | minor | §7 | «Полная сетка 16k × 31h» — состав сетки не определён (реферная §3.1 — 4 k × 3 h), источник чисел 16/31 не указан; критерий возврата «после WП-0/WП-2» не проверяем без состава | Определить перечень k и h полной сетки или дать явную ссылку на раздел v4 |
| C-16 | minor | Прил. A | Пример `c7.gap_rate.bps50` ссылается на метрику `gap_rate`, не определённую нигде (в §3.13/§4 нет формулы и знаменателя) | Заменить пример на определённую метрику (напр. `c7.top_year_share.bps50`) или определить `gap_rate` |
| C-17 | minor | §3.4 (граничные правила) | «adv ровно на последнем баре окна у no-hit → ADV_FIRST с пометкой» — вид и место «пометки» не специфицированы (поле? флаг в артефакте? публикация?) | Доопределить: имя флага/поля и где публикуется |
| C-18 | minor | §4 (инварианты), §3.12(2) | «максимум один match на episode_definition_id» двусмысленно (на группу или на эпизод?), при том что одна нога может содержать несколько групп (§3.12(2) говорит «максимум один эпизод на группу») | Переформулировать инвариант: «ровно один из шести статусов на группу; эпизод может матчить несколько групп» |
| C-19 | minor | §8 (контракт), §3.4 | Значения `touch_bar_adverse_bps` и `mae_through_touch_bar_bps` при no-hit (t_tp = −1) в fp-артефакте не специфицированы — e7 закрыл только pre-touch поля и маску; для touch-бар полей NaN-конвенция не записана | Доопределить по аналогии с e7: NaN при no-hit, чтение только под hit-маской |
| C-20 | minor | §5.1 | `Y_t = {order_tp_adv, order_direction, mae-поля, …}` — открытый перечень («…»); точный состав бутстреп-вектора Z не зафиксирован, что нарушает воспроизводимость реплик | Перечислить состав Y_t полностью |
| C-21 | minor | §5.3 | «Sensitivity к thinning» — thinning не определён (что прореживается, с каким шагом) | Определить процедуру или исключить из протокола |
| C-22 | minor | §4.1 (`episodes_per_year`) | При счёте эпизодов как sensitivity-range (§3.11) не операционализировано, какой счёт входит в ratio max/min полных лет: основной `resolved_episode_count`, `count_if_flagged_included` или границы диапазона конвенций | Указать: ratio считается по `resolved_episode_count` (или явно определить расчёт по границам) |
| C-23 | minor | §8(3), §3.2, §3.11, §8 (WП-2) | Ссылки на внешние уроки L4/L15/L20/L22/L24 без расшифровки при заявленной самодостаточности rev.5.3; требования detached-режима «по L24» в спеке не воспроизведены | Дать краткую расшифровку используемых уроков (особенно L24 для detached-режима) или убрать ссылки на внешний реестр |

SUMMARY: 1 critical / 9 major / 13 minor
