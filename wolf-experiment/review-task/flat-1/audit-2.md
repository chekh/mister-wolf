# Audit-2 — spec-v1.md (итерация 2)

Дата: 2026-08-17. Метод: повторный полный аудит обновлённой спеки по трём
перспективам с приоритетом полноты (edge cases, failure-режимы) и остаточных
as-built расхождений; code-ref/ сверка.

| ID | Severity | Перспектива | Раздел | Проблема | Предложение |
|---|---|---|---|---|---|
| F2-01 | major | sec | §8 | Политика невалидных входов модуля first-passage не зафиксирована: NaN/inf в OHLC, entry ≤ 0, high < low, row+k_tot за краем ряда, дубликаты уровней. В numba-ядре сравнения с NaN дают False — касания молча теряются (тихое искажение). As-built validate_inputs = production-стоп до numba. | Зафиксировать в §8: валидация входов до расчёта, любое нарушение — стоп; молчаливая фильтрация запрещена. |
| F2-02 | major | sec | §3.6 | Источник `window_len(delayed)` не определён: keys-кадр корпуса покрывает только якоря (бары 0…oos_cut−16); delayed_row может оказаться баром вне keys-кадра — там window_len отсутствует. Правильное значение выводится по позиции формулой §3.2 (`min(H_max, oos_cut − 1 − row)`), а не читается из keys. | Уточнить в §3.6: window_len для произвольного бара (в т.ч. delayed) вычисляется по формуле §3.2 от позиции, keys-кадр — только для якорей. |
| F2-03 | major | cmp | Прил. A | Конвенция суффиксов границ в `metric_id` не зарегистрирована: §3.8/§4.1 требуют публиковать lb/ub раздельными строками; as-built DICT_V42: `.lb` (share_min/P_min), `.ub` (share_max/P_max/(|B|+|U|)/(|A|+|U|)), `.incl_unresolved` (P+P(U)). Ссылка из §4 (правка F1-16) повисла. | Добавить в Прил. A конвенцию суффиксов (as-built) с расшифровкой и запретом слов pessimistic/optimistic. |
| F2-04 | major | cmp | §3.12(2) | Граничная тайл-семантика матча не полна: (a) confirm-бар и retracement-хвост `(ext_n, confirm_n)` принадлежат НОВОЙ ноге `[ext_n, ext_{n+1}]` (не вводится отдельное состояние unmatched); (b) вырожденная нулевая нога (start_row = ext_row — три ноги содержат один бар) — выбор ноги с БОЛЬШИМ индексом («новая»). As-built matching.py фиксирует оба правила; спека — нет. | Дополнить §3.12(2) обоими граничными правилами (as-built). |
| F2-05 | minor | sec | §3.3 | Предпосылка уникальности timestamps канонического ряда не зафиксирована: touch_id = ts[touch_row] и «группировка по touch_row эквивалентна группировке по touch_id» (as-built touch_groups) верны только при уникальных ts; дубликат ts молча склеит разные касания. | Добавить: timestamps ряда уникальны; дубликат — стоп на загрузке (контроль в гейте потребления/загрузке вселенной). |
| F2-06 | minor | cmp | §3.6 | Edge case δ ≥ h не описан: условие `0 < t_hit(t+δ) ≤ h−δ` невыполнимо → original/same пусты по построению (доли 0.0 при непустом знаменателе), renewed остаётся содержательным. As-built delay.py документирует; спека молчит — риск ложного QA-инцидента на «нулевых» долях. | Добавить в §3.6 явное граничное правило δ ≥ h. |
| F2-07 | minor | sec | §5.1 | Контрольные числа блоков: «drawn ≈ 150/75/37» — точно `ceil(N/ℓ)` для N = 1 507 711: 150/75/38 (для ℓ=40320 ceil(37.39)=38); nonoverlap-equivalent `floor(N/ℓ)` = 149/74/37. Числа проверяемы и должны быть точными (контрольные). | Уточнить: drawn = 150/75/38; nonoverlap = 149/74/37. |
| F2-08 | minor | cmp | §4.1 | `active_days_share`: знаменатель «наблюдаемые торговые сутки года» не определён (что такое сутки при 24/5 и data-gap: UTC-дата с ≥1 наблюдаемым баром scope?). | Определить: календарная UTC-дата, содержащая ≥1 наблюдаемый бар scope. |
| F2-09 | minor | cmp | §3.13 | Сборка ATR(H1) не определена: из каких баров собирается H1-слот (календарный час UTC? что при пропущенных барах внутри слота — NaN ATR и исключение события?). | Зафиксировать: H1-слот — календарный час UTC по наблюдаемым M1-барам; слот с неполным покрытием баров → ATR = null, событие не классифицируется по ATR-порогу (bps-пороги остаются). |
| F2-10 | minor | con | §3.13 vs code-ref | «crosses_weekend (переход через ≥1 календарный Sat/Sun)» — не определён интервал проверки; as-built gaps._spans_weekend: дни полуинтервала `(prev_date, curr_date]`. При переносе в другой код возможен другой край. | Уточнить: Sat/Sun строго между timestamp[i−1] и timestamp[i] (календарные дни (prev_date, curr_date]). |
| F2-11 | minor | cmp | §3.5/§3.6 | Контракт «один якорь на бар в ячейке» (row_index уникальны; дубль — стоп) as-built (touch_groups, delay) в спеке не зафиксирован, хотя на нём держится биекция «якорь → ровно одна группа» из инварианта публикации §4. | Добавить в §3.5: вселенная ячейки — не более одного якоря на bar row_index; дубль — стоп (production-политика). |

SUMMARY: 0 critical / 4 major / 7 minor
