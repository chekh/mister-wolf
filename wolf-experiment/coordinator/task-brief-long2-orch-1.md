# Task Brief: LONG-002 / orch-1 — nanohttp

## Metadata

- Задача: LONG-002, итерация orch-1 (оркестрованный прогон, архитектурная задача)
- Корень проекта: /Users/chekh/Development/RestAdviser/ZetaFlow/wolf-experiment
- Рабочий каталог задачи: long-task-002/orch-1/
- Отчёт: executor/report-long2-orch-1.md

## Task

Построить минималистичный веб-фреймворк **nanohttp** с нуля **ПОЛНОСТЬЮ ПО СПЕЦИФИКАЦИИ**
`long-task-002/orch-1/spec.md`.

Обязательно: прочитать спецификацию **целиком** — она содержит:
- 8 модулей с точными сигнатурами и описанием поведения;
- edge cases;
- обязательную структуру пакета;
- **Приложение A** — 40 пунктов публичного API;
- **Приложение B** — обязательные тесты с точными именами.

## Acceptance Criteria

1. Пакет `nanohttp/` и тесты `tests/` созданы в `long-task-002/orch-1/` строго
   по обязательной структуре из spec.md.
2. Используется **только stdlib** Python (никаких внешних зависимостей).
3. Все 40 пунктов публичного API из Приложения A реализованы с точными
   сигнатурами.
4. Все обязательные тесты из Приложения B написаны с точными именами и
   проходят.
5. Из каталога `long-task-002/orch-1`:
   `python3 -m unittest discover -s tests -t .` — все тесты зелёные.
6. Пример из раздела «Пример использования» spec.md запускается и работает.

## Constraints

- Обмен между уровнями — только файлами (brief/report/код).
- Executor не пишет код сам — только через воркеров. Декомпозиция с таблицей:
  архитектурные компоненты (MEDIUM/COMPLEX) — воркерам; тривиальное (склейка,
  __init__, конфиги) — себе.
- Обязательное ревью worker-reviewer после реализации; правки — до зелёных тестов.
- Не выходить за пределы каталога wolf-experiment/; код писать только в
  long-task-002/orch-1/ (workers/) и в отчёт executor/.
- Только stdlib; никаких сетевых действий, кроме локального запуска примера
  для проверки.
