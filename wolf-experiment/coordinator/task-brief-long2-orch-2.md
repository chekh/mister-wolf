# Task Brief: LONG-002 / orch-2 — веб-фреймворк nanohttp

## Metadata

- **ID:** LONG-002, итерация orch-2 (оркестрованный прогон, архитектурная задача)
- **Корень проекта:** `/Users/chekh/Development/RestAdviser/ZetaFlow/wolf-experiment/`
- **Рабочий каталог задачи:** `long-task-002/orch-2/`
- **Спецификация (источник истины):** `long-task-002/orch-2/spec.md`
- **Отчёт executor'а:** `executor/report-long2-orch-2.md`

## Task

Построить минималистичный веб-фреймворк **nanohttp** с нуля **ПОЛНОСТЬЮ ПО
СПЕЦИФИКАЦИИ** `long-task-002/orch-2/spec.md`:

- Прочитать спецификацию целиком:
  - 8 модулей с точными сигнатурами;
  - поведение и edge cases;
  - обязательная структура пакета;
  - приложение A — 40 пунктов публичного API;
  - приложение B — обязательные тесты с точными именами.
- Требования:
  - пакет `nanohttp/` + тесты `tests/`;
  - только стандартная библиотека Python (stdlib);
  - из каталога `long-task-002/orch-2`:
    `python3 -m unittest discover -s tests -t .` — **все тесты зелёные**;
  - пример из раздела «Пример использования» спецификации **работает**.

## Процедура исполнения

1. **Декомпозиция** с таблицей в плане executor'а:
   - архитектурные компоненты (модули с нетривиальным поведением, edge cases)
     — воркерам уровня **MEDIUM/COMPLEX**;
   - тривиальное (заглушки, `__init__`, простые структуры) — executor может
     делать сам (по правилам эксперимента — через минимальных воркеров, если
     сам не имеет права писать код; решить по своему регламенту).
2. **Обязательное ревью** воркером `worker-reviewer` после интеграции:
   соответствие спецификации (приложение A — все 40 пунктов API, приложение
   B — все обязательные тесты с точными именами).
3. **Тесты до зелёных**: прогон `python3 -m unittest discover -s tests -t .`
   из каталога `long-task-002/orch-2`; падения — возврат на доработку.
4. Проверить пример из раздела «Пример использования».
5. Обмен только файлами (brief / report / код), не выходить за пределы
   `wolf-experiment/`.

## Acceptance Criteria

1. Спецификация `spec.md` прочитана целиком; реализация покрывает все 8
   модулей с точными сигнатурами.
2. Покрытие приложения A: все 40 пунктов публичного API реализованы.
3. Покрытие приложения B: все обязательные тесты присутствуют с точными
   именами.
4. `python3 -m unittest discover -s tests -t .` из `long-task-002/orch-2` —
   все зелёные (0 failures, 0 errors).
5. Пример из раздела «Пример использования» запускается и работает.
6. Только stdlib — никаких внешних зависимостей.
7. Пройдено ревью worker-reviewer (с вердиктом в отчёте).

## Constraints

- Не выходить за пределы каталога `wolf-experiment/`.
- Код — только в `long-task-002/orch-2/` (пакет `nanohttp/`, тесты `tests/`).
- Только stdlib Python 3.
- Обмен между уровнями — только файлами.
- Воркеры не спавнят агентов; executor не пишет архитектурный код сам —
  только через воркеров (тривиальное — по его регламенту).
- В отчёте executor'а указать: статус, декомпозиция (сам vs воркеры),
  покрытие API (приложение A), покрытие тестов (приложение B), результат
  прогона unittest, вердикт worker-reviewer.
