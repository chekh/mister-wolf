# Report — REVIEW-001 / orch-1 (executor-author)

- **Задача:** итеративное ревью спеки `review-task/orch-1/spec-v0.md` (1043 строки), 3 раунда с изоляцией перспектив: security → completeness → consistency.
- **Результат:** spec-v1.md → spec-v2.md → spec-v3.md (rev.5.3.1 → rev.5.3.4); спека выросла до 1312 строк (все правки — точечные errata + Changelog-блоки, структура/нумерация/русский язык сохранены).
- **Дата:** 2026-08-18.

## Сводка по раундам

| Раунд | Перспектива | Вход | Найдено | Применено | Issues |
|---|---|---|---|---|---|
| 1 | security | spec-v0 (1043) | 0 c / 6 m / 6 min | **12/12** (6 major + 6 minor) | `issues/round-1.md` |
| 2 | completeness | spec-v1 (1128) | 0 c / 8 m / 20 min | **28/28** (8 major + 20 minor) | `issues/round-2.md` |
| 3 | consistency (+code-ref) | spec-v2 (1246) | 0 c / 4 m / 6 min | **8/10** (3 major + 5 minor; 1 major и 1 minor — частично/отклонены, см. ниже) | `issues/round-3.md` |
| **Итого** | | | **0 c / 18 m / 32 min** | **48/50** | |

## Раунд 1 — security (SEC-1…SEC-12, применены все)

**Главные изменения (spec-v1, errata s1–s12):**
- **s1:** NaN запрещён во всех parquet-артефактах раунда (включая `first_passage/fp_{run_id}.parquet`); материализация пустых значений — единая: null + маска `pre_touch_window_empty`; NaN-поля as-built ядра маскируются в null при потреблении; тест «NaN отсутствует в артефакте».
- **s2:** контроль целостности fp-артефакта: атомарная запись (tmp → os.replace + fsync), sha256 в `run_manifest.json`, проверка при каждом чтении.
- **s3:** новый инвариант *OOS containment* (§4, §8): последний бар окна каждого якоря строго левее oos_cut; ни одно поле артефактов не вычисляется по барам ≥ oos_cut (защита prospective holdout).
- **s4:** preregistration-механика: sha256/commit спеки + зоны DT, пороги хвостов, длины блоков — в манифесте; изменение порога — только новой ревизией спеки.
- **s5:** изоляция smoke-прогонов (`*_smoke`), атомарная замена каталогов c* файлом целиком, список артефактов с sha256 в манифесте.
- **s6:** правило нулевого знаменателя для балансов §3.7/`range_B` (null + insufficient + inconclusive).
- **s7–s12 (minor):** алфавит `run_id` (UTC ISO + `[a-z0-9-]`, защита путей); пин версий numba/numpy/polars + правило побитовой сверки только при совпадении окружения с эталоном WП-0; ATR при отсутствии закрытого слота — null + `atr_unavailable` + доля баров без ATR; слепая разметка Face-validity QA (фиксация sha256 до сравнения); supervisor останова (kill + `aborted` + `.partial`, запрет параллельных прогонов); ожидаемая связь контрольных чисел в манифесте.

**Отклонённые:** нет.

## Раунд 2 — completeness (CMP-1…CMP-28, применены все)

**Главые изменения (spec-v2, errata c1–c28):**
- **c1:** no-hit конвенция (t_tp = −1) контракта модуля: touch/pre-touch величины → null, `t_pre_touch_mae` → −1, `pre_touch_window_empty` = true; тест no-hit-строк.
- **c2:** `share_of_hits_heavy` и `eventual_target_after_adverse` зарегистрированы co-статистиками DT-строки терцильных долей (снято противоречие с §0); решающее правило гейта §3.9 операционализировано.
- **c3:** конвенция нулевого знаменателя распространена на все строки-отношения DT.
- **c4:** строгие определения категорий matching `unresolved_episode` / `init_or_censored` (какие состояния FSM порождают категорию).
- **c5:** определены H1/H4-слоты (календарный час UTC, «закрытый» слот) и конвенция B0.
- **c6:** бутстреп C2a — блоки ряда эпизодов (confirm timestamps); C2b/C2c — метод A на якорном ряду `Z_t` (запрет метода B распространён).
- **c7:** «attribution-sensitive» определён (доля same-bar U в терцильной поддержке ≥ 0.10, порог прегистрирован); зоны терцильной строки — дизъюнктное разбиение.
- **c8:** правило чтения sensitivity-range: зона ⟺ обе границы диапазона в одной зоне.
- **c9–c28 (minor):** исправлены числа блоков (drawn 150/75/38 vs nonoverlap 149/74/37); именованный флаг `adv_at_window_edge`; определения `onlyL/onlyS`; конвенция границ терцилей (полуоткрытые интервалы + клампинг включительно); артефакт `c8_decision_table.*`; `cost_model_id = 'none'` not null; определение «поддержанной строки» (= sufficient); «наблюдаемые торговые сутки» (UTC, ≥1 бар); Σ пустых месяцев — только полные годы (max 36); top-min(10, N_qualifying) + пометка; Σ|B_i| ≥ 100 — по Q1+Q3; база q95/q99 (все переходы, absolute gap); remaining horizon = h − t_adv; metric_id `c2b.delay_excluded_share`, `c7.atr_unavailable_share`; UNRESOLVED_LONG_SHORT по (k,h,dir) с a_bps = null; контроли §6 привязаны к наследию v4 (b0–b5); ограничение декларации самодостаточности; инициализация hi/lo в FSM-псевдокоде; минимальный объём QA-выборки (≥3 фрагмента/тип, ≥20 суток); `Z_FP_M1_MIN = 7200` в preregistration-списке.

**Отклонённые:** нет.

## Раунд 3 — consistency + code-ref (CON-1…CON-10, применено 8)

**Главные изменения (spec-v3, errata k1–k10):**
- **k1 (CON-1, частично):** as-built утверждения о ядре корпуса (`_path_fp_tau_zone`, Int32-материализация, D1-решётка) отнесены к модулю сборки c1_v4, отсутствующему в снапшоте code-ref; проверяемый контракт — артефакты WП-0 (§3.2, §3.4). Полное снятие ссылок отклонено: WП-0 (6/6 PASS) верифицировал контракт по артефактам, ссылки фиксируют происхождение конвенций.
- **k2 (CON-2, частично):** канон имён — контрактные имена §8; фактические имена прототипа (tau_up/dn_k*, mae_thru_*, k_tot, up/dn) — переходные, приводятся к контракту на шаге 1 WП-1 (материализация `t_adv` и `pre_touch_window_empty`, теги long/short); в Прил. A добавлена таблица соответствия имён contract ↔ prototype. Приведение §8 к текущему интерфейсу прототипа отклонено: контракт по построению фиксируется до кодинга и не следует за rev.1-прототипом.
- **k3 (CON-3):** разведены `window_len` (корпусная колонка) / `k_total` (внутреннее имя сборки) / `k_tot` (выход модуля v4.2); ссылка §4(ii) исправлена на `min(H_max, …)`.
- **k4 (CON-5, в спеке):** новый инвариант публикации — доли при нулевом знаменателе ≠ 0.0 (только null + insufficient).
- **k5 (CON-6):** зарегистрирован порог минимального окна якоря 15 баров — причина rows − anchors = 15 (сверено с `n_anchors = oos_cut − 15` в коде).
- **k6 (CON-8):** qualifying — свойство эпизода (amplitude ≥ k), из matched-групп не выводится (§3.12(5)).
- **k7 (CON-9):** порог терцильной поддержки унифицирован: `|A_i| ≥ 30` обоих сравниваемых (Q1, Q3).
- **k8 (CON-10):** MBB-реализация атласа v2 (`aggregate.py`: 500 реплик, block_len=16) объявлена непригодной для v4.2; модуль v4.2 — отдельный, параметры из манифеста.

**Отклонённые как правки спеки (передаются как замечания к коду; code-ref — read-only):**
- **CON-4 (major):** `cost_model_id: None` в `supply/catalog.py::build_record` противоречит c14 (`'none'`, not null). Спека уже фиксирует целевое поведение (c14, Прил. A); требуется правка кода: писать строку `"none"` и включить поле в `_NOT_NULL`.
- **CON-7 (minor):** `_NOT_NULL` реализации включает `horizon`/`grain` (спека: `horizon` nullable при неприменимости) и не фильтрует `k_bps` по grain. Требуется синхронизация кода с Прил. A; спека корректна, правке не подлежит.

Примечание: CON-5 также содержит замечание к коду (wp2_cells.py возвращает 0.0 при нулевом знаменателе, delay/matching — NaN; три конвенции) — спека закрывает это инвариантом публикации (k4), унификация кода — задача WП-1/WП-2.

## Acceptance Criteria — сверка

- `issues/round-1.md`, `round-2.md`, `round-3.md` — существуют, выводы ревьюеров дословно, таблица `| ID | Severity | Раздел | Проблема | Предложение |` + SUMMARY-строки. ✅
- `spec-v1.md`, `spec-v2.md`, `spec-v3.md` — существуют. ✅
- Ровно 3 раунда, порядок security → completeness → consistency. ✅
- `spec-v0.md` и `code-ref/` не изменялись; ничего не удалено; структура, нумерация разделов и русский язык сохранены; правки точечные, история — в Changelog-блоках. ✅
