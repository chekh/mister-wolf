# PLAYGROUND-ISSUES — реестр проблем, найденных на площадке

Живой реестр дефектов и трения, обнаруженных при догфудинге Wolf на `playground/`
(чистый снапшот кода). Сырой протокол сценариев: `playground/MANUAL-NOTES.md`
(материал для пользовательского мануала). Статусы: `open` → `in-brief` (диспетчено)
→ `fixed` / `rejected` (с причиной).

| ID | Дата | Сценарий | Тип | Описание | Статус |
|---|---|---|---|---|---|
| F1 | 2026-09-01 | Установка | UX/DOC | Глобальный `wolf` оказался симлинком на dev-копию (`mr-wolf@npm:mister-wolf -> /Users/chekh/Development/mister-wolf`) — версия всегда равна рабочему дереву, обновление не тестируется. Мануалу нужен способ отличать линк от реальной установки (стрелка `->` в `npm ls -g`). | open |
| F2 | 2026-09-01 | Установка | Q владельцу | В реестре живёт легаси-пакет **`mr-wolf@0.1.1`** (старое имя): `npm i -g mr-wolf` ставит НЕ тот пакет. Судьба имени: депрекация/redirect/занять? | open |
| F3 | 2026-09-01 | Обновление | DOC | Путь обновления `npm rm -g` → `npm i -g mister-wolf@latest` работает чисто (28 пакетов). В мануал: ожидаемый вес, проверка `npm ls -g mister-wolf` без `->`. | open |
| F4 | 2026-09-01 | `wolf init` | **BUG** | Первый init свежего проекта НЕ пишет платформенный конфиг (`opencode.json` с MCP-сервером): «platform configs: skipped — no platform detected» — детекция не видит каталог `.opencode/`, который сам init создаёт строками выше. MCP подключается только при ВТОРОМ запуске («platform opencode: written»). Чинить: детекция после рендера набора или дефолт opencode. | open |
| F5 | 2026-09-01 | `wolf init` | UX | Лог init не показывает имена скиллов: 13 строк «SKILL.md created» — наблюдаемость нулевая. Писать имя скилла/путь. | open |
| F6 | 2026-09-01 | `wolf init` | UX | Misleading-сообщения: «platform configs: skipped» звучит как «агенты не установлены» (набор отрендерен); «Restart your agent platform to pick up the MCP server» печатается даже когда MCP не настроен. | open |
| F7 | 2026-09-01 | `wolf init` | DOC | Init не сообщает про `opencode.json`: без второго запуска непонятно, что нужен шаг подключения MCP. В мануал явный шаг + в help/init-вывод подсказку. | open |
| F8 | 2026-09-01 | `wolf init` | **Q владельцу / UX** | Init автоматически выполняет полный scan проекта: 97 документов снапшота легли в `.wolf/memory/shared/documents/doc_*.md` — память выглядит «перенесённой из основного проекта», 107 файлов на чистом init. Предложение: scan при init — опционально (`--scan`) или лёгкий (только скелет + базовый набор), полный scan — отдельная осознанная команда `wolf scan`. | open |

## Принятые решения по находкам

(заполняется по мере триажа владельцем)
